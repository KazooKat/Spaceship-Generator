# Changelog

All notable changes to this project are documented here.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

- docs(bench-ci): add Sample GitHub Actions workflow section with self-contained yaml snippet for bench script invocation + CSV artifact upload (feat-docs-bench-ci-extension)
- test(properties): add palette x wing_style x seed-grid stability property test (feat-tests-property-palette-x-wing-style-grid)
- docs(palette_authoring): add Designing a biome pack section covering cohesion, avoiding overlap, naming, cross-axis test coverage (feat-docs-palette-authoring-extension-2)
- docs(troubleshooting): extend with --config-dump schema drift, --from-manifest replay drift, palette x engine cross-axis failures, web UI preset drawer corruption, biome pack name collision (feat-docs-troubleshooting-extension-5)
- docs(glossary): extend with --from-manifest, biome-pack-catalog, palette-x-hull-cross-axis-test, web-UI-presets-drawer entries (feat-docs-glossary-extension-5)
- docs(faq): extend with 4 Q&As on palette x hull cross-axis test, --config-dump vs --from-manifest, palettes Biome packs section, web UI palette/preset picker (feat-docs-faq-extension-5)
- test(properties): add palette x engine_style x seed-grid stability property test (feat-tests-property-palette-x-engine-style-grid)
- docs(recipes): extend with 4 recipes on --config-dump capture, --config-load replay, biome-pack mass-generation loop, single cross-axis test node targeting (feat-docs-recipes-extension-6)
- feat(palettes): add tundra_aurora, crimson_canopy biome palettes (feat-palettes-biome-pack-2026-05-12c)
- docs(web_ui): add Picking a palette and preset walkthrough section (feat-docs-web-ui-extension)
- docs(palettes): add Biome packs section cataloging the 2-palette biome-themed pack series (feat-docs-palettes-extension)
- docs(configuration): extend with --config-dump / --config-load round-trip examples and override semantics (feat-docs-configuration-extension)
- test(properties): add palette x hull_style x seed-grid stability property test (feat-tests-property-palette-x-hull-style-grid)
- docs(output-formats): extend with amulet-core consumer, --stats-json + pandas, jq-based ship binary diff snippets (feat-docs-output-formats-extension-2)
- test(properties): add cockpit_style x hull_style x seed-grid stability property test (feat-tests-property-cockpit-x-hull-style-grid)
- docs(bench): add CI integration section covering bench script invocation, CSV capture, awk/python regression diff (feat-docs-bench-extension-2)
- feat(palettes): add shadow_citadel, golden_savannah biome palettes (feat-palettes-biome-pack-2026-05-12b)
- docs(quickstart): add Next steps section linking to palettes, presets, recipes, cli, architecture (feat-docs-quickstart-extension-2)
- docs(presets): extend with annotated preset YAML schema, CLI selection, composing flags on top of preset, custom preset for fleets (feat-docs-presets-extension)
- test(properties): add greeble_style x greeble_density x seed-grid stability property test (feat-tests-property-greeble-style-x-density-grid)
- docs(troubleshooting): extend with entries on palette pack name collision, cross-axis test id collisions, jq quoting on PowerShell, cross-link footer drift, extreme greeble_density values (feat-docs-troubleshooting-extension-4)
- feat(palettes): add obsidian_forge, prismatic_reef biome palettes (feat-palettes-biome-pack-2026-05-12a)
- docs(recipes): extend with 4 recipes on diff-only palette_lint CI gate, cross-axis property test scaffold template, --meta-json + --output-json combined pipeline, architecture cross-link table tooling map (feat-docs-recipes-extension-5)
- docs(glossary): extend with biome-palette-pack, cross-axis-test (extension), palette_lint-json-schema, cross-link-footer entries (feat-docs-glossary-extension-4)
- docs(faq): extend with 4 Q&As on cross-axis property tests, biome palette use-cases, palette_lint --json schema, architecture cross-link footer (feat-docs-faq-extension-4)
- feat(palettes): add arcane_library, mistwood_grove biome palettes (feat-palettes-biome-pack-2026-05-08e)
- test(properties): add `palette × greeble_density × seed`-grid stability property test in `tests/test_properties.py` — numeric-axis cross-product companion to the just-shipped enum/numeric cross-axis pack (cockpit×hull / cockpit×wing / hull×engine / engine×wing / cockpit×engine / hull×wing / palette×cockpit_style / greeble_density×weapon_count) and the palette-axis numeric-sibling of the existing palette × cockpit_style cross-axis test; pins (palette × `greeble_density` × seed) deterministically to catch regressions that only surface in the interaction between palette role coverage (palette YAML in `palettes/` → block-id mapping in `palette.py`) and the greeble scatter density (`greeble_density=` top-level kwarg → multi-cell scatter pass in `greeble_styles.scatter_greebles`) — e.g., a palette stubbing the `greeble` role combined with `greeble_density=1.0` claiming every surface anchor with a block id the palette cannot map (silent no-op → degenerate output) — that would slip past both single-axis siblings (`test_property_palette_seed_grid_*` and `test_property_greeble_density_seed_grid_*`); slices the palette list dynamically (first / middle / last alphabetically of `_PALETTE_NAMES`, already `sorted(p.stem for p in palettes_dir().glob("*.yaml"))` so adding a new YAML to `palettes/` automatically tracks the slice) via the existing `_slice_first_middle_last` helper × three densities (`0.0` no-greebles bare-hull / `0.5` mid / `1.0` max scatter — same extremes as the `test_property_greeble_density_x_weapon_count_seed_grid` sibling and the `test_property_greeble_density_monotonic_in_block_count` companion) × the small fixed seed grid `[0, 1, 7]` (`_SHAPE_STYLE_STABILITY_SEEDS`) for 3 × 3 × 3 = 27 representative nodes; plumbs `greeble_density` via the top-level `generate(greeble_density=...)` kwarg (NOT via `ShapeParams`) since the top-level kwarg accepts the full `[0.0, 1.0]` range and matches the numeric-axis sibling `test_property_greeble_density_x_weapon_count_seed_grid` exactly; mirrors the cross-axis siblings — calls `generate()` writing `.litematic` to `tmp_path`, asserts the file exists with non-zero size and `block_count > 0`, and `pytest.fail` messages name the offending `(palette, greeble_density, seed)` tuple via parametrize IDs so a regression localizes immediately; placed adjacent to the palette × cockpit_style cross-axis test (immediately after it) and grows the suite by exactly 27 tests; full `pytest -q` + `ruff check .` both green (closes feat-tests-property-palette-x-greeble-density-grid)
- docs(troubleshooting): extend `docs/troubleshooting.md` with 6 new rows appended to the existing `## Common errors` table — covers (1) `--meta-json` mutex against `--output*` / `--config-dump` (parser.error → exit 2 + stderr diagnostic, fix is to use one flag at a time, cross-link `docs/cli.md#machine-readable-list--output-flags`), (2) `palette_lint.py --json` schema mismatch with consumer expectations (the script ships TWO JSON output flags — legacy `--format json` emitting `{name, path, errors, warnings}` and newer `--json` emitting `{palette, ok, errors, warnings}` — they are not interchangeable, fix is to pick the schema the consumer expects with `--json` for CI `.ok` gates and `--format json` for legacy tools, both support `--strict`), (3) texture pattern flags (`--window-period` / `--stripe-period` / `--rivet-period` / `--belly-light-period`) appearing silently ignored — period values are clamped to `max(2, value)` at the lower end and become silent no-ops at the upper end when the period exceeds the ship-axis length so `_z_phase_mask` / `_y_band_mask` produces zero hits; fix is to pick periods in `[2, ship_axis_length / 2]` and verify via `--stats-json` per-role block counts, cross-link `docs/architecture.md#texture-pipeline`, (4) `--output-json | jq` parse errors when stderr is folded into stdout via `2>&1` — CLI splits streams (JSON payload on stdout, progress/banner/warning lines on stderr, same convention as the `bench_*.py --csv` family) so `2>&1` interleaves non-JSON text into the document; fix is to drop `2>&1` or route streams separately to two files, cross-link `docs/output-formats.md#consumer-examples`, (5) fleet seed reproducibility — re-running the same `--seed --fleet-count N --out DIR` produces identical `ship_<seed>_<idx>.litematic` filenames that silently overwrite the previous run; the determinism is intentional (per-ship seeds drawn from a single `random.Random(--seed)` stream), fix is per-run `--out` directory or fresh `--seed`, cross-link `docs/architecture.md#fleet-pipeline`; each entry is a single row appended to the existing table (matches the `Symptom | Cause | Fix` shape verbatim, no restructure of pre-existing rows, code-fence-equivalent inline backticks for commands), 6 added lines (well under the 60-line cap); existing entries unchanged; full `pytest -q` + `ruff check .` both green on docs-only change (closes feat-docs-troubleshooting-extension-3)
- docs(recipes): extend `docs/recipes.md` with 4 new task-oriented recipes appended after Recipe 22 and before the `## See also` footer (so existing Recipes 1–22 are unchanged) — Recipe 23 (palette lint CI gate via `python scripts/palette_lint.py --json --all` wrapped in a shell one-liner that captures the JSON array, fails the build on any `ok==false` entry, and prints `palette: <error list>` for each dirty palette via `jq -r '.[] | select(.ok==false) | "\(.palette): \(.errors|join(\", \"))"'`, exit-1 propagated so CI gates branch correctly), Recipe 24 (`--meta-json` piped to `jq` for tooling discovery — `python -m spaceship_generator --meta-json | jq '{version, palettes: .palettes[].name}'` extracting just the fields tooling needs (`version` + palette names) instead of ten separate `--list-*-json` subprocesses, sibling of Recipe 19's `keys` example), Recipe 25 (Texture pipeline knob tuning via `--window-period 4 --stripe-period 6 --engine-glow-depth 2` showing the three flags plumb into `TextureParams` in the [Texture pipeline](architecture.md#texture-pipeline) to dial window cadence / accent-stripe period / engine glow recess depth without editing palette YAML), and Recipe 26 (debugging a cross-axis property test failure via `pytest -k engine_style_x_wing -v` so the offending `(axis_a, axis_b, seed)` parametrize tuple prints in the failure header for fast localization, cross-link [tests/test_properties.py](../tests/test_properties.py) and Recipe 16 for the naming convention); each recipe is one short paragraph + 1 concrete command in a code fence matching the existing recipe tone exactly (heading depth `### Recipe N — Title`); 43 added lines (under the 50-line cap); existing Recipes 1–22 and the `## See also` footer unchanged (closes feat-docs-recipes-extension-4)
- docs(architecture): add `## Cross-link index` H2 footer table to `docs/architecture.md` appended after `## Related documentation` (so existing per-component / pipeline-level sections + the Related-doc bullet list are unchanged) — 3-column `Pipeline | CLI flag(s) | Web API endpoint(s)` table mapping each documented pipeline to its driver CLI flag(s) and (where applicable) web API endpoint(s): Shape (`--seed --hull-style --engine-style --wing-style ...` → `POST /api/generate`), Hull / Wing / Greeble / Weapon / Cockpit / Engine / Structure pipelines each → their `--<x>-style` (or `--<x>-density` / `--<x>-count`) + `--list-<x>-styles[-json]` (or `--list-<x>-types[-json]`) flag pack + `GET /api/<x>-styles` (or `/api/<x>-types`) mirror, plus Fleet pipeline (`--fleet-count --out` → n/a, CLI only) and Texture pipeline (`--window-period --stripe-period --engine-glow-depth ...` → n/a, CLI tunables); each row pipeline name links to the existing in-doc anchor (`#shape-pipeline` etc.) so readers can jump from CLI/API surface back to the pipeline section; opens with one short cross-link paragraph pointing at `docs/cli.md` for the full flag reference and `docs/web_ui.md` for the API; 17 added lines (well under the 40-line cap); existing content unchanged; full `pytest -q` + `ruff check .` both green on docs-only change (closes feat-docs-architecture-cross-link-footer)
- docs(faq): extend `docs/faq.md` with 4 new Q&A entries appended after the existing `### How do I capture the exact effective config of a CLI invocation?` entry so the existing 16 Q&As are unchanged — covers (1) when to reach for `--meta-json` vs the individual `--list-*-json` flags (single-call combined-bundle discovery vs cherry-picking one enum, naming the combined keys `version`/`palettes`/`presets`/`hull_styles`/`engine_styles`/`wing_styles`/`cockpit_styles`/`structure_styles`/`greeble_types`/`weapon_types`/`roles`, the shared `--quiet` carve-out and the `--meta-json` mutex set, cross-link `docs/cli.md`), (2) how to run `palette_lint.py --json` in a CI gate (`--all --json` whole-corpus / `--file <path> --json` single-target form, the JSON object/array shape `{palette, ok, errors, warnings}`, the exit code semantics 0/1, the `--strict` warnings-as-errors combo, the canonical `jq '.[] | select(.ok | not)'` offender filter, cross-link `docs/recipes.md` Recipe 22), (3) what the Texture pipeline does at a glance (`assign_roles(shape_grid, params)` as the sole entry, the 10 `_paint_*` passes, the coarse→fine role refinement contract `HULL`/`COCKPIT_GLASS`/`ENGINE`/`WING`/`GREEBLE` → `INTERIOR`/`WINDOW`/`HULL_DARK`/`ENGINE_GLOW`/`LIGHT`, the `TextureParams` flag pack `--window-period`/`--stripe-period`/`--engine-glow-depth`/`--hull-noise-ratio`/`--panel-bands`/`--rivet-period`/`--engine-glow-ring`, cross-link `docs/architecture.md#texture-pipeline` and `docs/cli.md`), and (4) what the hull × wing cross-axis property test catches that single-axis tests don't (`test_property_hull_x_wing_style_seed_grid_*` in `tests/test_properties.py` pinning `HullStyle` × `WingStyle` × seed over the 27-node 3 × 3 × 3 grid, naming a concrete narrow-dagger × swept-gull anchor-column-outside-Z-band interaction-only regression that slips past both single-axis siblings, cross-link to the per-component Hull / Wing pipeline sections in `docs/architecture.md` and the existing generic cross-axis FAQ entry); each entry is one short paragraph matching the existing FAQ tone/cross-link pattern verbatim (`### How do I X?` / `### When ...?` / `### What ...?` heading depth, single-paragraph A); 15 added lines (well under the 60-line cap); existing Q&As unchanged; full `pytest -q` + `ruff check .` both green on docs-only change (closes feat-docs-faq-extension-3)
- docs(glossary): extend `docs/glossary.md` with 4 new entries for surfaces shipped in latest cycles — `--meta-json` (CLI flag emitting combined enum/palette/preset/version JSON document, CLI mirror of `/api/meta`, slotted between `--config-dump` and `--version-json`, cross-link `docs/cli.md#machine-readable-list--output-flags`), `Consumer examples` (the two copy-paste Python `litemapy` + shell `--output-json | jq` snippets in `docs/output-formats.md`, slotted between `Common pitfalls` and `cross-axis property test`, cross-link `docs/output-formats.md#consumer-examples`), `hull × wing cross-axis property test` (concept entry for the (HullStyle × WingStyle × seed) 27-node parametrize-grid test catching hull-silhouette × wing-planform interaction regressions, slotted between `GreebleType` and `HullStyle`, cross-link `docs/faq.md#why-do-cross-axis-property-tests-exist-alongside-single-axis-ones`), and `Reading the output` (bench-output semantics — `mean_ms` arithmetic mean / `p95_ms` 95th percentile / `TOTAL` aggregates across per-row sample pools NOT sum of per-row means — for the wall-clock `bench_*.py` family, slotted between `preset` and `Role`, cross-link `docs/bench.md#reading-the-output`); each entry inserted in its correct case-insensitive A–Z position so existing entries are unchanged; each entry one short paragraph (≤4 sentences) matching existing tone/cross-link pattern verbatim; 24 added lines (under the 25-line cap); existing entries unchanged; full `pytest -q` + `ruff check .` both green on docs-only change (closes feat-docs-glossary-extension-3)
- docs(architecture): add Texture pipeline section — extends `docs/architecture.md` with a new pipeline-level H2 `## Texture pipeline` between `## Fleet pipeline` and `## Related documentation` documenting `src/spaceship_generator/texture.py` (the role refinement stage between coarse shape grid and palette-driven block assignment, NOT a per-component pipeline so the per-component pipeline INDEX near the top is intentionally untouched — same decision as the just-shipped Fleet pipeline sibling); section covers `assign_roles(shape_grid, params)` as the sole public entry plus the 10 internal `_paint_*` passes (interior, accent stripe, panel bands, windows, hull noise, rivets, engine glow, engine glow ring, wing lights, belly lights, nose-tip light), the shared helpers (`_side_facing_mask`, `_z_phase_mask`, `_y_band_mask`, `_forbidden_mask`, `_coord_hash_mod1000`), the `_PROTECTED_ROLES` / `_HULL_NOISE_FORBIDDEN` overwrite-gate tuples, the role refinement contract (coarse `HULL`/`COCKPIT_GLASS`/`ENGINE`/`WING`/`GREEBLE` → fine `INTERIOR`/`WINDOW`/`HULL_DARK`/`ENGINE_GLOW`/`LIGHT`), `TextureParams` (the 9-field `dataclass` with `window_period_cells`/`accent_stripe_period`/`engine_glow_depth`/`belly_light_period`/`nose_tip_light`/`hull_noise_ratio`/`panel_line_bands`/`rivet_period`/`engine_glow_ring`) and how it plumbs through `generator.generate(..., texture_params=...)` (CLI builds it from `--window-period`/`--stripe-period`/`--engine-glow-depth`/`--hull-noise-ratio`/`--panel-bands`/`--rivet-period`/`--engine-glow-ring`, web constructs in `ship_support.py`'s 5-tuple builder); section length 80 lines (at the cap, matching the Engine/Structure/Fleet pipeline siblings); cross-links to `#shape-pipeline` (the coarse-role producer) and `docs/cli.md` for the texture flag pack; matches the pipeline-level prose/heading-depth convention + Mermaid diagram style; full `pytest -q` + `ruff check .` both green on docs-only change (closes feat-docs-architecture-texture)
- feat(scripts): add `--json` flag to `scripts/palette_lint.py` emitting a structured machine-readable lint payload (`{"palette": "<name>", "ok": true|false, "errors": [...], "warnings": [...]}`) — single-target form (`--file palettes/X.yaml --json`) emits one JSON object, whole-corpus form (`--all --json`) emits a JSON array of those objects (one per palette), exit code matches the existing fixed-width default mode (0 if every entry has `ok=true` / 1 otherwise) and `--strict --json` combines so warnings count toward the `ok` flag (warnings-as-errors); machine-readable companion to the existing fixed-width text output (default behavior, `_format_text` / `_run_all` paths) and the existing `--format json` flag (which retains its `name`/`path`/`errors`/`warnings` schema for backward compatibility, see `test_cli_format_json_parses`); designed for CI gates that branch on `jq -r '.ok'` or `jq -r '.[].ok'` rather than parsing the human-readable summary; 4 new tests in `tests/test_palette_lint.py` cover the clean-palette / dirty-palette / `--all` array / `--strict` flip cases (closes feat-scripts-palette-lint-json)
- feat(palettes): add abyssal_trench, sunset_horizon biome palettes (feat-palettes-biome-pack-2026-05-08d)
- docs(recipes): extend `docs/recipes.md` with 4 new task-oriented recipes appended after Recipe 18 and before the `## See also` footer (so existing Recipes 1–18 are unchanged) — Recipe 19 (single-call enum/palette/preset/version discovery via `python -m spaceship_generator --meta-json | jq 'keys'` covering the combined JSON bundle that mirrors the web `/api/meta` endpoint and replaces ten separate `--list-*-json` subprocesses), Recipe 20 (batch fleet runs via `--fleet-count N --seed <master>` showing per-ship seed derivation off a single `random.Random(--seed)` stream + `ship_<seed>_<idx>.litematic` naming, cross-linked to [Fleet pipeline](architecture.md#fleet-pipeline) for the underlying batch driver / per-ship seed planning truth source), Recipe 21 (consume `--output-json` with `jq` for shell-script extraction — `python -m spaceship_generator --output-json ... | jq -r '"\(.seed)\t\(.palette)\t\(.blocks)"'` extracting tab-separated rows usable in `awk` / spreadsheets, with the no-`2>&1` stream-split caveat preserved matching the `bench_*.py --csv` family convention), and Recipe 22 (CI gate via `python scripts/palette_lint.py --all --format json | jq '.[] | select(.errors | length > 0)'` emitting one machine-readable doc over every `palettes/*.yaml` with per-palette error / warning arrays for structured CI annotations, cross-link `feat-scripts-palette-lint-json` once shipped); each recipe is one short paragraph + 1 concrete command in a code fence matching the existing recipe tone exactly (heading depth `### Recipe N — Title`); 46 added lines (under the 50-line cap); existing Recipes 1–18 and the `## See also` footer unchanged (closes feat-docs-recipes-extension-3)
- docs(cli): document `--meta-json` flag in `docs/cli.md` — adds a new row to the existing `## Machine-readable list / output flags` table describing the combined bundle payload (`{version, palettes, presets, hull_styles, engine_styles, wing_styles, cockpit_styles, structure_styles, greeble_types, weapon_types, roles}` — every per-enum / palette / preset / version payload of the individual `--list-<x>-json` flags merged into one JSON document), names the `--quiet` carve-out (NOT silenced — same as the rest of the family) and the mutex set (`--output` / `--output-json` / `--output-json-schema` / `--config-dump` → `parser.error` → exit 2 + stderr), cross-links `GET /api/meta` as the web mirror in the existing `API mirror` column; reuses the existing row-based machine-readable / quiet-carve-out phrasing verbatim so the new row reads as a sibling of the surrounding `--list-<x>-json` siblings; existing content unchanged; ≤30 added lines; full `pytest -q` + `ruff check .` both green on docs-only change (closes feat-docs-cli-meta-json)
- feat(cli): add `--meta-json` — combined enum/palette/preset/version JSON for tooling — emits a single JSON document containing every per-enum / palette / preset / version payload the individual `--list-<x>-json` flags emit (`version`, `presets`, `palettes`, `hull_styles`, `engine_styles`, `wing_styles`, `cockpit_styles`, `structure_styles`, `greeble_types`, `weapon_types`, `roles`), CLI mirror of the web `/api/meta` endpoint; each value sourced from the SAME function / enum that the matching `--list-<x>-json` flag uses (`list_palettes()`, `_presets.SHIP_PRESETS`, `HullStyle` / `EngineStyle` / `WingStyle` / `CockpitStyle` / `StructureStyle` / `GreebleType` / `_weapon_styles.WeaponType` / `Role`, `__version__`) so adding a palette or preset is automatically reflected with no hard-coded lists; emits via direct `print(..., file=sys.stdout)` so `--quiet --meta-json` still prints (same carve-out as `--stats-json` / `--list-presets-json` / `--list-shape-styles-json`); mutually exclusive with `--output`, `--output-json`, `--output-json-schema`, and `--config-dump` (rejected via `parser.error` → exit 2 + stderr); argparse declaration sits adjacent to the `--list-*-json` / `--version-json` siblings in `build_parser` so the help-text grouping reads as one machine-readable family; 4 new tests in `tests/test_cli.py` mirror the existing `test_cli_list_<x>_json_*` patterns and cross-check each key against the matching `--list-<x>-json` payload (closes feat-cli-meta-json)
- test(properties): add `hull_style × wing_style × seed`-grid stability property test in `tests/test_properties.py` — cross-axis companion to the just-shipped enum cross-axis pack (cockpit×hull / cockpit×wing / hull×engine / engine×wing / cockpit×engine / palette×cockpit) and a sibling of the single-axis `hull_style_seed_grid` and `wing_style_seed_grid` parametrize tests; pins (`HullStyle` × `WingStyle` × seed) deterministically to catch regressions that only surface in the interaction between hull silhouette (`hull_style=` top-level kwarg → hull profile in `structure_styles.py`) and wing placement (`ShapeParams.wing_style` → wing placer dispatch in `shape/wings.py`) — e.g., a narrow dagger hull combined with a swept-back gull wing where the wing's stern anchor column falls outside the dagger's thin Z-band — that would slip past both single-axis siblings (`test_property_hull_style_seed_grid_*` and `test_property_wing_style_seed_grid_*`); slices each enum to the first three members in declaration order via `list(HullStyle)[:3]` / `list(WingStyle)[:3]` (sourced dynamically off the enums so the slice tracks future reorderings) for 3 × 3 × 3 = 27 representative nodes; mirrors the engine×wing and cockpit×engine cross-axis siblings exactly — `hull_style` passed directly to `generate()`, `wing_style` plumbed via `ShapeParams.wing_style`, asserts the `.litematic` exists with non-zero size and `block_count > 0`, and `pytest.fail` messages name the offending `(hull_style, wing_style, seed)` tuple via parametrize IDs so a regression localizes immediately; placed adjacent to the cockpit×engine cross-axis test (immediately after it) and grows the suite by exactly 27 tests; full `pytest -q` + `ruff check .` both green (closes feat-tests-property-hull-x-wing-style-grid)
- feat(palettes): add ember_forge, twilight_glade biome palettes (feat-palettes-biome-pack-2026-05-08c)
- docs(bench): extend `docs/bench.md` with a new `## Reading the output` H2 section appended between the existing `## How to read the output` short footer and the `## Aggregate snapshot` section so existing content (Catalog table, How-to-read paragraph, Aggregate snapshot, CSV format, per-script breakdown, Quick reference) is unchanged — adds a representative 5-row fixed-width-table example showing what `bench_summary.py` prints by default (`bench | metric | iterations` columns, mix of `ms` and `mb` units, mix of high and low iteration counts) followed by a one-paragraph explanation of the per-row / TOTAL row semantics across the wall-clock bench family: `mean_ms` is the arithmetic mean of per-iteration timings, `p95_ms` is the 95th-percentile of those same samples, and the final `TOTAL` row aggregates across every per-row sample pool (stages / palettes / densities / ships) — explicitly NOT a sum of per-row means; one-sentence callouts note that `bench_mem.py` substitutes MB units (`mean_mb` / `p95_mb` / `max_mb`) and `bench_generator.py` swaps the shape for a cProfile-attributed `phase | total_s | mean_s | pct` table where `pct` is the phase share of total wall and `WALL TOTAL.mean_s` is wall-per-ship; section ends with a cross-link to `docs/bench-ci.md` for CI ingest patterns; 28 added lines (well under the 50-line cap); existing sections unchanged; full `pytest -q` + `ruff check .` both green on docs-only change (closes feat-docs-bench-extension)
- docs(output-formats): extend `docs/output-formats.md` with a new `## Consumer examples` H2 section appended after the existing `## Differences between CLI and web payloads` content (so existing `## .litematic schematic` / `## CLI JSON output` / `## Web API JSON response` sections are unchanged) — provides two concrete copy-paste snippets a downstream user can run today: (1) a 5-line Python snippet using `litemapy` (already a runtime dep at `litemapy>=0.11.0b0` per `pyproject.toml`, so no extra install) that calls `Schematic.load(...)`, grabs the single `Region` via `next(iter(schem.regions.values()))`, and prints `region.count_blocks()` — matches the `blocks` field emitted by `--output-json` for the same ship and points at `src/spaceship_generator/export.py` as the writer side, (2) a shell snippet running `python -m spaceship_generator --output-json --seed 42 --palette sci_fi_industrial --out out/ | jq -r '"\(.seed)\t\(.palette)\t\(.blocks)"'` extracting `seed` / `palette` / `blocks` as a tab-separated row, with the no-`2>&1` caveat preserved (errors stay on stderr — same stdout/stderr stream-split convention as the `bench_*.py --csv` family); cross-links to existing in-doc anchors `#litematic-schematic` and `#cli-json-output---output-json` and to `docs/cli.md` for `--output-json` / `--output-json-schema` per-flag reference; 36 added lines (well under the 60-line cap); existing content unchanged; full `pytest -q` + `ruff check .` both green on docs-only change (closes feat-docs-output-formats-extension)
- docs(quickstart): extend `docs/quickstart.md` with a new `## 6. Running a fleet` section appended after the existing single-ship walkthrough (sections 1-5 unchanged) and before the existing `## Next steps` footer — covers the `--fleet-count N` batch mode shipped in `feat-cli-fleet-count`, shows the concrete invocation `python -m spaceship_generator --fleet-count 3 --seed 42 --out fleet_out`, names the per-ship output naming convention `ship_<seed>_<idx>.litematic` (zero-based `<idx>`, per-ship `<seed>` pulled from a single `random.Random(--seed)` stream so the whole fleet is reproducible from one master seed), and notes that re-running with the same `--seed` + `--out` overwrites the previous fleet (matches the troubleshooting entry); cross-links to `docs/cli.md` for the full fleet flag pack (`--fleet-size-tier`, `--fleet-style-coherence`, ...) and to the just-shipped [Fleet pipeline](architecture.md#fleet-pipeline) section in `docs/architecture.md` for the underlying batch driver / per-ship seed / dims / style planning truth source; section heading depth `## N. Title` matches the existing 5 numbered sections verbatim, prose tone matches the surrounding "## 5. Launch the web UI" sibling (one short intro paragraph + a single-command code fence + one short trailing paragraph naming the output convention + one cross-link paragraph); 12 added lines (well under the 40-line cap); existing sections 1-5 and the `## Next steps` footer unchanged; full `pytest -q` + `ruff check .` both green on docs-only change (closes feat-docs-quickstart-extension)
- docs(glossary): extend `docs/glossary.md` with 5 new entries for surfaces shipped in the latest cycles — `palette_search.py` (authoring helper that walks every `palettes/*.yaml` and prints one `(palette, role)` pair per match for a target Minecraft block id, slotted between `palette_merge.py` and `palette_stats.py`, cross-link `docs/recipes.md#recipe-15--find-every-palette-referencing-a-minecraft-block-id`), `Structure pipeline` (the assembly pipeline rooted at `StructureStyle` — the *coarsest* dial scaling every later stage of the shape build, slotted between `ShapeParams` and `StructureStyle`, cross-link `docs/architecture.md#structure-pipeline`), `Common pitfalls` (the five mistakes new palette authors hit most often — missing `minecraft:` namespace, role-name typo, non-existent block id, YAML quoting slips, non-cube in solid-cube role, slotted between `CockpitStyle` and `EngineStyle`, cross-link `docs/palette_authoring.md#common-pitfalls`), `cross-axis property test` (concept entry for the pytest parametrize-grid tests that pin two ship-axis dials together for 27 nodes per pair, slotted between `Common pitfalls` and `EngineStyle`, cross-link `docs/faq.md#why-do-cross-axis-property-tests-exist-alongside-single-axis-ones` + `tests/test_properties.py`), and `Fleet pipeline` (batch ship-build driver in `src/spaceship_generator/fleet.py` planning per-ship seed/dims/styles deterministically, slotted between `EngineStyle` and `gen_id`, cross-link `docs/architecture.md#fleet-pipeline`); each entry is one short paragraph (≤4 sentences) matching the existing tone/cross-link pattern verbatim, inserted in its correct case-insensitive A–Z position so existing entries are unchanged; 30 added lines (at the cap); existing entries unchanged; full `pytest -q` + `ruff check .` both green on docs-only change (closes feat-docs-glossary-extension-2)
- docs(troubleshooting): extend `docs/troubleshooting.md` with 5 new rows appended to the existing `## Common errors` table — covers (1) `palette_search.py --block <id>` prints "0 hit(s)" and exits 1 → explains the missing-`minecraft:`-prefix mistake and the deliberate exit-code semantics (0 hit / 1 no-hit / 2 bad-args) so CI pipelines can branch on `$?`, (2) `palette_search.py --block <id>` prints dozens of hits → re-run with `--csv` for a parseable `palette,role` stream pipeable into `sort` / `awk` / a spreadsheet, with the stderr-banner stream-split convention preserved (matches the `bench_*.py --csv` family), (3) per-`StructureStyle` override list missing from `docs/architecture.md#structure-pipeline` → point at `src/spaceship_generator/structure_styles.py` as the truth source for the per-style `if style == StructureStyle.<X>` branches in `profile_fn` / `hull_rx_ry_scale` / `engine_count_override` / `engine_radius_scale` / `wing_prob_override` / `wing_size_scale` (architecture doc names the seams but doesn't enumerate every numeric value to avoid doc drift), (4) fleet `.litematic` files overwriting a previous run in the same `--out` directory → `--fleet-count N` uses `ship_<seed>_<idx>.litematic` so re-running with the same `--seed` into the same `--out` silently overwrites; fix is per-run `--out` directory or fresh seed, (5) authoring a new palette and hitting silent failures (missing role, bad block id, role typo, indent error) → cross-link [palette_authoring.md#common-pitfalls](palette_authoring.md#common-pitfalls) for the 5-mistake catalog and the canonical `palette_lint.py --strict` (`--all --strict` for whole-corpus) "catches this" tool; each entry is a single row appended to the existing table (matches the `Symptom | Cause | Fix` shape verbatim, no restructure of pre-existing rows, code-fence-equivalent inline backticks for commands), 5 new lines added (well under the 60-line cap); cross-links into `docs/architecture.md` and `docs/palette_authoring.md` rather than duplicating their content; the optional 6th cross-axis-property-test entry was skipped because it is already present in the table from `feat-docs-troubleshooting-extension`; existing entries unchanged; full `pytest -q` + `ruff check .` both green on docs-only change (closes feat-docs-troubleshooting-extension-2)
- test(properties): add `cockpit_style × engine_style × seed`-grid stability property test in `tests/test_properties.py` — cross-axis companion to the just-shipped enum cross-axis pack (cockpit×hull / cockpit×wing / hull×engine / engine×wing / palette×cockpit) and a sibling of the single-axis `cockpit_style_seed_grid` and `engine_style_seed_grid` parametrize tests; pins (`CockpitStyle` × `EngineStyle` × seed) deterministically to catch regressions that only surface in the interaction between cockpit placement (`ShapeParams.cockpit_style` → cockpit placer dispatch in `shape/cockpit.py`) and engine placement (`engine_style=` top-level kwarg → engine builder dispatch in `engine_styles.py`) — e.g., a tall canopy/dome cockpit silhouette combined with a wide quad-cluster engine layout where the engine anchor row crowds the cockpit's Z-band at the stern transition — that would slip past both single-axis siblings (`test_property_cockpit_style_seed_grid_*` and `test_property_engine_style_seed_grid_*`); slices each enum to the first three members in declaration order via `list(CockpitStyle)[:3]` / `list(EngineStyle)[:3]` (sourced dynamically off the enums so the slice tracks future reorderings) for 3 × 3 × 3 = 27 representative nodes; mirrors the engine×wing cross-axis sibling exactly — `cockpit_style` plumbed via `ShapeParams.cockpit_style`, `engine_style` passed directly to `generate()`, asserts the `.litematic` exists with non-zero size and `block_count > 0`, and `pytest.fail` messages name the offending `(cockpit_style, engine_style, seed)` tuple via parametrize IDs so a regression localizes immediately; placed adjacent to the engine×wing cross-axis test (immediately after it) and grows the suite by exactly 27 tests; full `pytest -q` + `ruff check .` both green (closes feat-tests-property-cockpit-x-engine-style-grid)
- feat(palettes): add scarlet_oasis, frostbite_tundra biome palettes (feat-palettes-biome-pack-2026-05-08b)
- docs(recipes): extend `docs/recipes.md` with 4 new task-oriented recipes appended after Recipe 14 and before the `## See also` footer (so existing Recipes 1–14 are unchanged) — Recipe 15 (cross-corpus palette search via `scripts/palette_search.py --block <id>` + `--csv` variant for spreadsheet / CI ingest, blockstate-suffix-stripping noted, cross-linked to `docs/palette_authoring.md`), Recipe 16 (run a single cross-axis property test via `pytest -q -k engine_style_x_wing` / `cockpit_x_hull` / `palette_x_cockpit_style` showing the `test_property_<axis_a>_x_<axis_b>_seed_grid_*` naming convention so authors can target one of the 6 cross-axis pairs at 27 nodes instead of the full ~700-test suite), Recipe 17 (per-component pipeline navigation pointing at the [Per-component pipelines](architecture.md#per-component-pipelines) anchor index near the top of `docs/architecture.md` that links each `## <Component> pipeline` section — Shape/Hull/Wing/Greeble/Weapon/Cockpit/Engine/Structure — so readers click through instead of scrolling), and Recipe 18 (whole-corpus palette validation pre-commit gate via `python scripts/palette_lint.py --all --strict` covering the `--all` whole-corpus form + `--strict` warnings-as-errors flag); each recipe is one short paragraph + 1–3 concrete commands in a code fence matching the existing recipe tone exactly (heading depth `### Recipe N — Title`); 48 added lines (under the 50-line cap); existing Recipes 1–14 and the `## See also` footer unchanged (closes feat-docs-recipes-extension-2)
- docs(architecture): add Fleet pipeline section — extends `docs/architecture.md` with a new pipeline-level H2 `## Fleet pipeline` between `## Structure pipeline` and `## Related documentation` documenting `src/spaceship_generator/fleet.py` (the batch ship-build driver, NOT a per-component pipeline so the per-component pipeline INDEX near the top is intentionally untouched); section covers `generate_fleet(params: FleetParams) -> list[GeneratedShip]` as the sole public driver, the `FleetParams` / `GeneratedShip` / `SIZE_TIERS` / `dims_in_tier` entry points, how `--fleet-count > 1` plumbs from `cli.py` into per-ship `generate()` calls via `_run_fleet_ship(planned, idx=i, args=args)` (which forwards planned per-ship dims/palette/styles onto a shallow-copied `argparse.Namespace` and invokes the same `_run_one` path solo runs use), the per-ship seed derivation (NOT arithmetic `base + i` offsetting — every per-ship seed is `rng.randrange(0, 2**31 - 1)` pulled from a single `random.Random(params.seed)` stream that also drives dims/hull/engine/wing/greeble draws so determinism is end-to-end), and the output naming convention `ship_<seed>_<idx>.litematic` (forced inside `_run_fleet_ship` so `--filename` cannot collide across fleet ships, schematic name defaults to `Ship <seed>` for distinct in-game Litematica browser entries); section length 77 lines (under the 80-line cap, matching the Engine/Structure pipeline siblings); cross-links to `docs/cli.md#repeat--fleet-modes` (`--fleet-count`, `--fleet-size-tier`, `--fleet-style-coherence`) and `scripts/bench_fleet.py` (wall-clock micro-benchmark of the multi-ship fleet path); matches the per-component pipeline section style for prose/heading depth + Mermaid diagram convention; full `pytest -q` + `ruff check .` both green on docs-only change (closes feat-docs-architecture-fleet)
- docs(palette_authoring): extend `docs/palette_authoring.md` with a new `## Common pitfalls` section appended after `## Testing a new palette` (so existing schema / role / preview-colors / testing content is unchanged) — covers 5 mistakes new palette authors hit most often: (1) missing `minecraft:` namespace prefix on a block id, (2) role name typo / using a role not in the `Role` enum schema (`HULL_LIGHT`, `COCKPIT`, `WINDOWS` etc.), (3) non-existent / removed-in-MC-version block id (loader validates *syntax* only, not existence — silent missing-block at Litematica load), (4) YAML indent / quoting errors (unquoted `#hex` colors parsed as comments, mixed tabs/spaces, trailing colons), (5) block id pointing at a non-cube block (slab/stairs/fence/pane) for a role that expects a solid 1x1x1 cube (`HULL` / `HULL_DARK` / `WING` / `ENGINE` / `GREEBLE` / `INTERIOR`); each entry is a single H3 with one short paragraph of fix guidance matching the existing prose tone (compare with `## Picking harmonious preview colors` numbered list above); section opens with a one-paragraph cross-link to `scripts/palette_lint.py --strict` (and the `--all --strict` whole-corpus form) as the canonical "catches this" tool so authors have a single command to run after every edit; 23 added lines (well under the 60-line cap); existing content unchanged; full `pytest -q` + `ruff check .` both green on docs-only change (closes feat-docs-palette-authoring-pitfalls)
- docs(faq): extend `docs/faq.md` with 4 new Q&A entries appended after the existing `### Where do I find the list of every CLI flag?` entry so the existing 14 Q&As are unchanged — covers (1) which palette tooling script to reach for (one-paragraph overview of the `scripts/palette_*.py` family naming `palette_lint.py` / `palette_stats.py` / `palette_diff.py` / `palette_merge.py` / `palette_to_json.py` and what each is for, cross-link `docs/palette_authoring.md` and `docs/recipes.md`), (2) why cross-axis property tests exist alongside single-axis ones (referencing the existing `test_property_*_x_*_seed_grid` pattern in `tests/test_properties.py` — cockpit×hull / cockpit×wing / hull×engine / greeble_density×weapon_count / palette×cockpit_style — and what interaction-only regressions they catch that single-axis siblings miss), (3) how to navigate `docs/architecture.md` for one specific component (point at the per-component pipeline index near the top with anchor links to each `## <Component> pipeline` section), and (4) how to capture the exact effective config of a CLI invocation via `--config-dump` (emits `{"effective_config":{...}}` post preset-resolution, mutex set, `--quiet` carve-out, replayable, cross-link `docs/cli.md`); each entry is one short paragraph matching the existing FAQ tone/cross-link pattern verbatim (`### How do I X?` heading depth, single-paragraph A); 16 added lines (well under the 60-line cap); existing Q&As unchanged (closes feat-docs-faq-extension-2)
- docs(architecture): add Structure pipeline section — extends `docs/architecture.md` with a new H2 `## Structure pipeline` between `## Engine pipeline` and `## Related documentation`, completing the per-component pipeline series (Shape/Hull/Wing/Greeble/Weapon/Cockpit/Engine/**Structure**); section describes `structure_styles.py` (`StructureStyle` `StrEnum` with 6 members `FRIGATE`/`FIGHTER`/`DREADNOUGHT`/`SHUTTLE`/`HAMMERHEAD`/`CARRIER` + the per-style override functions `profile_fn` / `hull_rx_ry_scale` / `engine_count_override` / `engine_radius_scale` / `wing_prob_override` / `wing_size_scale`), the `apply_hull_style(grid, hull_style)` and `default_cockpit_for(structure_style, cockpit_style)` entry points, and the role of `structure_style` in the assembly pipeline (where it slots in — at the *base* of every per-component pipeline — and what it scales: hull profile + rx/ry, engine count and radius, wing probability and span/thickness/length, cockpit default); matches the style + heading depth + Mermaid diagram convention of the existing siblings (Hull/Wing/Greeble/Weapon/Cockpit/Engine pipeline sections); cross-links to `docs/cli.md --list-structure-styles` and `docs/web_ui.md /api/structure-styles`; ALSO adds `[Structure pipeline](#structure-pipeline)` line to the per-component pipeline INDEX near the top of the doc; section length exactly 80 lines (at the cap, matching the Engine pipeline sibling); full `pytest -q` + `ruff check .` both green on docs-only change (closes feat-docs-architecture-structure)
- feat(palettes): add meteor_shower, solar_corona biome palettes (feat-palettes-biome-pack-2026-05-08)
- feat(scripts): add palette_search.py — search palettes by Minecraft block id (feat-scripts-palette-search)
- docs(glossary): extend `docs/glossary.md` with 6 new entries for surfaces shipped after the original glossary — `--config-dump` (CLI flag emitting effective generator config as JSON, cross-link `docs/cli.md`), `--version-json` (machine-readable version sibling of `--version`, cross-link `docs/cli.md`), `/api/version` (REST endpoint returning `{"version":"X.Y.Z"}`, cross-link `docs/web_ui.md`), and the three palette authoring helpers `palette_diff.py` / `palette_merge.py` / `palette_stats.py` (cross-link `docs/recipes.md`); each entry inserted in its correct alphabetical position relative to the existing case-insensitive A–Z order so existing entries are unchanged (`--config-dump` and `--version-json` slot ahead of the existing leading-`.` `.litematic` entry, `/api/version` slots between `.litematic` and `CockpitStyle`, the three `palette_*.py` entries slot between the existing `palette` and `preset` entries); each entry is one short paragraph (≤4 sentences) matching the existing tone/cross-link pattern verbatim (cross-links use the exact deep-dive section anchors — e.g. `cli.md#effective-config-dump`, `web_ui.md#discovery--metadata`, `recipes.md#recipe-11--diff-two-palettes`); section length 40 added lines (at the cap); existing entries unchanged (closes feat-docs-glossary-extension)
- test(properties): add `palette × cockpit_style × seed`-grid stability property test in `tests/test_properties.py` — cross-axis companion to the just-shipped enum/numeric cross-axis pack (cockpit×hull / cockpit×wing / hull×engine / greeble_density×weapon_count); pins (palette × `CockpitStyle` × seed) deterministically to catch regressions that only surface in the interaction between palette role coverage (palette YAML in `palettes/` → block-id mapping in `palette.py`) and cockpit placement (`ShapeParams.cockpit_style` → cockpit placer dispatch in `shape/cockpit.py`) — e.g., a palette missing or stubbing the `cockpit` role combined with a cockpit placer that emits a role variant the palette can't map (silent no-op → zero-block .litematic) — that would slip past both single-axis siblings (`test_property_palette_seed_grid_*` and `test_property_cockpit_style_seed_grid_*`); slices the palette list dynamically (first / middle / last alphabetically of `_PALETTE_NAMES`, already `sorted(p.stem for p in palettes_dir().glob("*.yaml"))` so adding a new YAML to `palettes/` automatically tracks the slice) and `CockpitStyle` to first / middle / last via the existing `_slice_first_middle_last` helper for 3 × 3 × 3 = 27 representative nodes; mirrors the cross-axis siblings exactly — calls `generate()` writing `.litematic` to `tmp_path`, asserts the file exists with non-zero size and `block_count > 0`, and `pytest.fail` messages name the offending `(palette, cockpit_style, seed)` tuple via parametrize IDs so a regression localizes immediately; placed adjacent to the greeble_density×weapon_count cross-axis test (immediately after it) and grows the suite by exactly 27 tests; full `pytest -q` + `ruff check .` both green (closes feat-tests-property-palette-x-cockpit-style-grid)
- feat(palettes): add obsidian_shore, starlight_temple biome palettes (feat-palettes-biome-pack-2026-05-07e)
- feat(scripts): add palette_to_json.py — convert palette YAML to JSON for tooling (feat-scripts-palette-to-json)
- docs(cli): document `--list-{engine,hull,wing}-styles[-json]` flags in `docs/cli.md` — extends the existing `## Machine-readable list / output flags` block (added in `feat-docs-cli-list-json-flags`) with three new rows for the per-component narrower siblings of `--list-shape-styles-json` shipped in `feat-cli-flags-pack-2026-05-07` (commit `00027a2`); each row notes the JSON shape (`{"engine_styles":[...]}` / `{"hull_styles":[...]}` / `{"wing_styles":[...]}`) and the matching `GET /api/<component>-styles` mirror; a short trailing paragraph names the non-json siblings, the `--quiet` carve-out (json variants exempt, non-json silenced), and the per-pair mutex (`parser.error` → exit 2) — same wording as the surrounding family; existing content unchanged; ≤30 added lines (closes feat-docs-cli-list-narrow-styles)
- docs(architecture): add per-component pipeline index at top of doc (feat-docs-architecture-pipeline-index)
- test(properties): add `greeble_density × weapon_count × seed`-grid stability property test in `tests/test_properties.py` — numeric-axis cross-product companion to the just-shipped enum cross-axis pack (cockpit×hull / cockpit×wing / hull×engine in `feat-tests-property-cross-axis-pack-2026-05-07`); pins three densities (`0.0` no-greebles bare-hull / `0.5` mid / `1.0` max scatter) × three weapon counts (`0` no-weapons / `2` mid / `8` max) × the small fixed seed grid `_SHAPE_STYLE_STABILITY_SEEDS = [0, 1, 7]` for 3 × 3 × 3 = 27 representative nodes that hit the extremes of both numeric axes; mirrors the cross-axis siblings exactly — calls `generate()` writing `.litematic` to `tmp_path`, asserts the file exists with non-zero size and `block_count > 0`, and `pytest.fail` messages name the offending `(greeble_density, weapon_count, seed)` tuple via parametrize IDs so a regression localizes immediately; placed adjacent to the hull×engine cross-axis test (immediately after it); catches a regression that only surfaces in the interaction between a specific density and a specific count (e.g., a maxed-out greeble scatter at `greeble_density=1.0` claiming every surface anchor so the weapon scatter at `weapon_count=8` cannot find empty cells to write into and silently no-ops) that would slip past both single-axis siblings (`test_property_greeble_density_seed_grid_*` and `test_property_weapon_count_seed_grid_*`); grows the suite by exactly 27 tests; full `pytest -q` + `ruff check .` both green (closes feat-tests-property-greeble-density-x-weapon-count-grid)
- feat(scripts): add palette_merge.py — merge two palettes with configurable strategy (feat-scripts-palette-merge)
- docs(presets): extend `docs/presets.md` with a new `## Preset parameter breakdown` section appended after `## Adding a new preset` so existing content (the role catalog table, `## Usage from Python`, `## Inspecting the raw table`, and `## Adding a new preset`) is unchanged — adds a one-paragraph intro that names `src/spaceship_generator/presets.py` as the truth source, calls out that `palette` is NOT a preset field (separate `generate(...)` kwarg), and points at `--list-presets-json` for the machine-readable variant; the table itself has one row per preset alphabetically (`battlecruiser`, `capital_carrier`, `corvette`, `dropship`, `freighter_heavy`, `gunship`, `interceptor`, `science_vessel`, `scout`) with columns `preset | hull / engine / wing / cockpit | size (w,h,l) | weapon_count | greeble_density | weapon_types` (the four style columns squashed into one cell separated by `/` to keep the table readable, weapon_types as a comma-separated list with `(none)` for `dropship`/`freighter_heavy`); enum values use the lowercase `StrEnum` `.value` form to match what `--list-presets-json` emits via `default=str` so the doc and the JSON sibling cannot visually drift; section is closed with a one-liner pointing at `python -m spaceship_generator --list-presets-json` and cross-linking `docs/cli.md`; values verified row-by-row against the `SHIP_PRESETS` dict; 26 added lines (well under the 80-line cap) (closes feat-docs-presets-extension)
- feat(palettes): add autumn_canopy, glow_lagoon biome palettes (feat-palettes-biome-pack-2026-05-07d)
- docs(bench-ci): extend `docs/bench-ci.md` with a new `## Example CI snippets` section appended after the existing `## Caveat: runner variance` content — provides three copy-paste stanzas: (1) a GitHub Actions job using `actions/setup-python@v5` + `pip install -e .` that runs `python scripts/bench_summary.py --csv > bench-summary.csv` and uploads the CSV via `actions/upload-artifact@v4`, (2) a GitLab CI mirror using `python:3.12` + `artifacts: paths:` keyword, (3) a regression-detection GitHub Actions job that runs `bench_generator.py --save current.json`, diffs against a committed `bench/baseline.json` via `bench_compare.py --csv`, and fails the job via an `awk` one-liner parsing the `delta_pct` column (field 4) when any phase exceeds 10%; documents the stdout-CSV / stderr-banner stream-split convention with the explicit "do not `2>&1`" caveat (matches the just-shipped troubleshooting entry); cross-links to `docs/bench.md`'s `## CSV output format` for column / row semantics so the snippets stay short; pure-docs change, no real workflow files added under `.github/workflows/`; section length 70 added lines (under the 80-line cap); existing `## What it does` / `## How to read the comment` / `## Updating the baseline...` / `## Caveat: runner variance` sections unchanged (closes feat-docs-bench-ci-extension)
- docs(cli): document `--config-dump` flag in `docs/cli.md` — adds a new `## Effective config dump` section after the existing `## Machine-readable list / output flags` section covering purpose (emit preset-resolved generator args that WOULD be passed into `generate()` as `{"effective_config":{...}}`, exit 0 without producing a ship), the mutex set (`--output`, `--output-json`, `--output-json-schema`), the `--quiet` carve-out (same as the `--list-*-json` family), and a short example invocation `python -m spaceship_generator --config-dump --seed 42 --palette desert_oasis` with abridged output; reuses the existing single-row table style + "machine-readable / quiet carve-out" phrasing verbatim so the new section reads as a sibling of the surrounding flag docs; existing content unchanged; ≤30 added lines (closes feat-docs-cli-config-dump)
- feat(cli): add `--config-dump` — emits the effective generator config (preset-resolved palette/shape/style/weapon args that WOULD be passed into `generate()`) as a single JSON document `{"effective_config":{...}}` to stdout via direct `print(..., file=sys.stdout)` so `--quiet --config-dump` still prints (same carve-out as `--stats-json` / `--list-presets-json` / `--list-shape-styles-json`), then exits 0 without producing a ship; useful for debugging / reproducing weirdness ("what did I actually run") since the dump runs after `--preset` resolution and `--palette random` resolution so it reflects the actual values `generate()` receives; mutually exclusive with `--output`, `--output-json`, and `--output-json-schema` (rejected via `parser.error` → exit 2 + stderr); argparse declaration sits adjacent to the `--list-*-json` / `--version-json` siblings in `build_parser` so the help-text grouping reads as one machine-readable family; 3 new tests in `tests/test_cli.py` mirror the existing `test_cli_list_<x>_json_*` patterns (closes feat-cli-config-dump)
- feat(scripts): add palette_diff.py — role-by-role palette diff with --csv (feat-scripts-palette-diff)
- feat(palettes): add enchanted_grove, asteroid_belt biome palettes (feat-palettes-biome-pack-2026-05-07c)
- docs(troubleshooting): extend `docs/troubleshooting.md` with 6 new entries appended to the existing `## Common errors` table — covers (1) `unrecognized arguments: --list-engine-styles` (and the rest of the per-component `--list-*-styles[-json]` / `--version-json` pack from cycle 2 of 2026-05-07) → version-check + upgrade reminder pointing at `--version-json`, (2) `bench_*.py --csv` output empty / interleaved when piping → stdout-CSV vs stderr-banner stream split (mistake `... --csv > out.csv 2>&1` vs correct `... --csv > out.csv`, cross-link to `docs/bench.md`), (3) `palette_stats.py` reports fewer palettes than `ls palettes/` shows → strict-loader silently drops broken palettes, surface offenders with `palette_lint.py --all --strict`, (4) `GET /api/version` returns `0.0.0+unknown` → editable-install fallback for `importlib.metadata.version("spaceship-generator")`, fix is non-editable install or read `__version__` directly (cross-link to `docs/web_ui.md`), (5) `palette_diff.py` shows `<missing>` for a role → role defined in only one palette, run `palette_lint.py --strict` to find which roles are MANDATORY, (6) cross-axis property test fails with a specific `(style_a, style_b, seed)` tuple → reproduce with `pytest -k "<test_name> and <style_a>-<style_b>-<seed>"`; each entry is a single row appended to the existing table (matches the `Symptom | Cause | Fix` shape verbatim, no restructure of pre-existing rows, code-fence-equivalent inline backticks for commands), 6 new lines added (well under the 60-line cap); cross-links into `docs/cli.md`, `docs/bench.md`, and `docs/web_ui.md` rather than duplicating their content; existing entries unchanged; full `pytest -q` + `ruff check .` both green on docs-only change (closes feat-docs-troubleshooting-extension)
- docs(recipes): extend `docs/recipes.md` with 4 new task-oriented recipes covering recently-shipped surfaces — Recipe 11 (palette diff via `scripts/palette_diff.py` + `--csv` variant, cross-linked to `docs/palette_authoring.md`), Recipe 12 (palette corpus audit via `scripts/palette_stats.py` + `--csv` / `--top 5` variants, cross-linked to `docs/palette_authoring.md`), Recipe 13 (programmatic server-version check via `curl http://localhost:5000/api/version` for web clients and `python -m spaceship_generator --version-json` for shell pipelines, cross-linked to `docs/web_ui.md` and `docs/cli.md`), and Recipe 14 (CI bench ingest via `python scripts/bench_summary.py --csv > bench-summary.csv` plus per-script `bench_*.py --csv` invocations, cross-linked to `docs/bench.md` and `docs/bench-ci.md`); each recipe is one short paragraph + 1–3 concrete commands in a code fence matching the existing recipe tone exactly; appended after Recipe 10 and before the `## See also` footer so existing recipes are unchanged; section length 47 added lines (under the 50-line cap) (closes feat-docs-recipes-extension)
- test(properties): add (cockpit x wing) + (hull x engine) cross-axis seed-grid stability property tests in `tests/test_properties.py` — companions to the just-shipped cockpit×hull cross-axis sibling, both reuse the existing `_slice_first_middle_last(EnumCls)` helper and the `_SHAPE_STYLE_STABILITY_SEEDS = [0, 1, 7]` constant for 3 × 3 × 3 = 27 nodes each (cockpit×wing pins (CockpitStyle × WingStyle × seed); hull×engine pins (HullStyle × EngineStyle × seed)); each new test mirrors the cockpit×hull sibling exactly — calls `generate()` writing `.litematic` to `tmp_path`, asserts the file exists with non-zero size and `block_count > 0`, and `pytest.fail` messages name the offending `(<axis1>, <axis2>, seed)` tuple via parametrize IDs so a regression localizes immediately; placed adjacent to the cockpit×hull test (immediately after it) and grows the suite by exactly 2 × 27 = 54 tests (closes feat-tests-property-cross-axis-pack-2026-05-07)
- feat(scripts): add palette_diff.py — role-by-role palette diff with --csv (feat-scripts-palette-diff)
- feat(cli): add `--list-engine-styles[-json]`, `--list-hull-styles[-json]`, `--list-wing-styles[-json]`, and `--version-json` flags — closes the engine/hull/wing CLI gap (each enum already exposed via `/api/engine-styles`, `/api/hull-styles`, `/api/wing-styles` but not as a narrower CLI sibling of `--list-shape-styles`) and adds a machine-readable `--version-json` companion to `--version`; each non-json flag prints one enum value per line in declaration order via the existing `_emit` funnel (silenced by `--quiet` like the other `--list-<x>` siblings), each `-json` flag emits a single JSON document `{"<x>":[...]}` directly via `print(..., file=sys.stdout)` so `--quiet --list-<x>-json` still prints (same carve-out as `--stats-json` / `--list-presets-json` / `--list-shape-styles-json`); each pair is mutually exclusive (rejected via `parser.error` → exit 2 + stderr); `--version-json` emits `{"version": __version__}` matching whatever `--version` prints (sourced from the same `spaceship_generator.__version__` attribute) and is mutually exclusive with `--version` — the check runs before `parse_args` because argparse's `action="version"` short-circuits during parsing and would otherwise win the race; argparse declarations placed adjacent to the existing `--list-shape-styles[-json]` / `--list-cockpit-styles[-json]` / `--list-structure-styles[-json]` / `--list-greeble-types[-json]` / `--list-weapon-types[-json]` siblings in `build_parser` so the help-text grouping reads as a sibling pack; 21 new tests in `tests/test_cli.py` (3 per flag pair × 3 list pairs + 3 for `--version-json`) follow the existing `test_cli_list_<x>_json_*` patterns exactly — each pulls the enum values dynamically from the source module so the two paths cannot drift (closes feat-cli-flags-pack-2026-05-07)
- docs(bench): document --csv output format for every bench_*.py script (feat-docs-bench-csv-format)
- feat(scripts): add palette_stats.py — cross-corpus palette stats with --csv (feat-scripts-palette-stats)
- test(properties): add cockpit_style x hull_style x seed-grid stability property test (feat-tests-property-cockpit-x-hull-style-grid)
- feat(palettes): add desert_oasis, foggy_marsh biome palettes (feat-palettes-biome-pack-2026-05-07b)
- docs(architecture): add Engine pipeline section (feat-docs-architecture-engines)
- docs(glossary): add docs/glossary.md term reference (feat-docs-glossary)
- feat(api): add GET /api/version — exposes installed package version to clients (feat-api-version)
- feat(palettes): add volcanic_island, crystal_caves biome palettes (feat-palettes-biome-pack-2026-05-07)
- feat(scripts): add --csv flag to bench_greeble_density.py for CI/spreadsheet ingest (feat-bench-greeble-density-csv)
- feat(scripts): add --csv flag to bench_mem.py for CI/spreadsheet ingest (feat-bench-mem-csv)
- feat(scripts): add --csv flag to bench_generator.py for CI/spreadsheet ingest (feat-bench-generator-csv)
- docs(architecture): add Wing pipeline section (feat-docs-architecture-wing)
- feat(palettes): add warm_savanna, frozen_river biome palettes (feat-palettes-biome-pack-2026-05-05e)
- feat(api): add GET /api/roles — narrower JSON sibling exposing Role enum to tooling (feat-api-roles)
- docs(output-formats): add docs/output-formats.md explaining .litematic + JSON payload shapes (feat-docs-output-formats)
- test(properties): add weapon_count x seed-grid stability property test (feat-tests-property-weapon-count-grid)
- docs(architecture): add Hull pipeline section (feat-docs-architecture-hull)
- feat(cli): add --list-roles / --list-roles-json — expose Role enum to tooling (feat-cli-list-roles)
- feat(scripts): add --csv flag to bench_full_pipeline.py for CI/spreadsheet ingest (feat-bench-full-pipeline-csv)
- feat(palettes): add eroded_badlands, magma_chamber biome palettes (feat-palettes-biome-pack-2026-05-05d)
- docs(recipes): add docs/recipes.md task-oriented recipe index (feat-docs-recipes)
- test(properties): add greeble_density x seed-grid stability property test (feat-tests-property-greeble-density-grid)
- feat(scripts): add --all flag to palette_lint.py — lint every palettes/*.yaml in one invocation (feat-palette-lint-all-flag)
- docs(configuration): add docs/configuration.md config-by-category index (feat-docs-configuration)
- feat(palettes): add dawn_meadow, glacial_blue biome palettes (feat-palettes-biome-pack-2026-05-05c)
- feat(scripts): add --csv flag to bench_shape.py for CI/spreadsheet ingest (feat-bench-shape-csv)
- docs(architecture): add Cockpit pipeline section (feat-docs-architecture-cockpit)
- test(properties): add (no_greebles, no_weapons) combos x seed-grid stability property test (feat-tests-property-no-greebles-no-weapons-combos)
- feat(cli): add --list-palettes-json — machine-readable variant of --list-palettes (feat-cli-list-palettes-json)
- feat(scripts): add --csv flag to bench_fleet.py for CI/spreadsheet ingest (feat-bench-fleet-csv)
- test(properties): add preset x seed-grid stability property test (feat-tests-property-preset-grid)
- feat(palettes): add sunflower_plains, stony_peaks biome palettes (feat-palettes-biome-pack-2026-05-05b)
- docs(architecture): add Weapon pipeline section (feat-docs-architecture-weapons)
- docs(cli): document the --list-*-json machine-readable flag family (feat-docs-cli-list-json-flags)
- feat(cli): add --list-cockpit-styles-json / --list-structure-styles-json / --list-greeble-types-json / --list-weapon-types-json — machine-readable variants (feat-cli-list-json-pack-2026-05-05)
- feat(api): add GET /api/hull-styles, /api/engine-styles, /api/wing-styles — narrower JSON siblings of /api/styles (feat-api-narrow-style-endpoints)
- feat(scripts): add --csv flag to bench_compare.py for CI/spreadsheet ingest (feat-bench-compare-csv)
- feat(palettes): add bamboo_jungle, flower_forest biome palettes (feat-palettes-biome-pack-2026-05-05)
- docs(contributing): add docs/contributing.md short development guide (feat-docs-contributing)
- test(properties): add palette x seed-grid stability property test (feat-tests-property-palette-grid)
- feat(cli): add `--output-json-schema` flag — emits the JSON Schema (Draft-7) for the `--output-json` payload to stdout, exits 0; lets downstream consumers programmatically validate the per-ship JSON document `--output-json` produces (one object per generated ship, NDJSON in bulk runs); schema is a top-level `type: object` with `required: ["seed","palette","shape","blocks","path"]` and `properties` covering the exact key set `_print_json_summary` emits (`seed: integer`, `palette: string`, `shape: array of 3 integers`, `blocks: integer`, `path: string`); declared as a module-level `_OUTPUT_JSON_SCHEMA` dict adjacent to `_print_json_summary` in `src/spaceship_generator/cli.py` so adding a new field to one path forces a sibling update to the other (both are kept in lockstep by the new `test_cli_output_json_schema_is_valid_jsonschema` test which checks the schema's `properties` covers `_REQUIRED_KEYS` from `tests/test_cli.py`); structurally aligned with `_OPENAPI_COMPONENTS["GenerateResult"]` (the canonical web-API schema in `src/spaceship_generator/web/blueprints/ship.py`) — both share `seed/palette/shape/blocks` keys with matching JSON types — but the CLI payload diverges on the file-output keys (`path` for the local `.litematic` filesystem path vs. the web API's `download_url` / `preview_url` / `gen_id` URL endpoints), so the schema is hand-written rather than derived (importing the web blueprint's component dict would couple `cli.py` to the optional `flask` web blueprint, which the CLI deliberately doesn't import); argparse declaration `--output-json-schema` (action `store_true`) placed immediately after `--output-json` in `build_parser` so the sibling pair stays adjacent in the help-text grouping; short-circuit handler in `cli.main` placed immediately after the `--list-shape-styles-json` handler (alongside the other `--*-json` short-circuits); deliberately NOT routed through `_emit` so `--quiet --output-json-schema` still prints — same carve-out as `--stats-json` / `--output-json` / `--list-presets-json` / `--list-shape-styles-json`, documented in the help text (`"NOT silenced by --quiet."`); two new tests in `tests/test_cli.py` cover (a) `test_cli_output_json_schema_is_valid_jsonschema` — `pytest.importorskip("jsonschema")`, then `json.loads(captured.out)` returns a dict with a `$schema` containing `draft-07` (or `draft/2020`+ for forward-compatibility), `type: "object"`, a `properties` dict whose keys are a superset of `_REQUIRED_KEYS = {"seed","palette","shape","blocks","path"}` (sourced from the existing module-level constant so the two paths can never drift), plus `jsonschema.Draft7Validator.check_schema(schema)` to verify the schema itself is well-formed (catches malformed type names, broken `$ref`, etc.), and (b) `test_cli_output_json_schema_quiet_still_emits` — `--quiet --output-json-schema` still emits the JSON Schema document (carve-out parallels `--quiet --output-json` / `--quiet --stats-json` / `--quiet --list-presets-json` / `--quiet --list-shape-styles-json`) (closes feat-cli-output-json-schema)
- docs(architecture): add Greeble pipeline section to `docs/architecture.md` — companion to the existing Shape pipeline section (shipped in `feat-docs-shape-pipeline`); covers (1) `greeble_styles.py` with the 11-member `GreebleType` `StrEnum` (`TURRET`, `DISH`, `VENT`, `ANTENNA`, `PANEL_LINE`, `SENSOR_POD`, `CIRCUIT_BOARD`, `BATTLE_DAMAGE`, `PIPE_CLUSTER`, `ORGANIC_GROWTH`, `NANO_MESH`) and the `scatter_greebles(shape, rng, density, *, types=None) -> list[Placement]` entry point that samples surface anchors via `_surface_anchors_from_grid` (true top-facing skin when `shape` is the live numpy grid) or `_surface_anchors` (bounding-box approximation when `shape` is a `(W, H, L)` tuple), draws a Bernoulli mask whose draw is independent of the `types` allow-list size (so changing the allow-list never reshuffles which anchors fire), and concatenates per-anchor placements; (2) `shape/greebles.py` with the in-shape `_place_greebles` 1-voxel-bump pass + vectorized `_surface_mask` helper that runs *before* the final mirror so bumps are bilaterally symmetric in the returned grid; (3) the role of `greeble_density` (capped at `0.5` on `ShapeParams.greeble_density` in `__post_init__` for the in-shape bump pass, full `[0.0, 1.0]` range on `generate(greeble_density=...)` for the multi-cell scatter pass, plumbed from CLI `--greeble-density` and the web `greeble_density` form/JSON field in `web/blueprints/ship.py`) and `greeble_types` (optional allow-list eagerly validated in `generator.py`, `None` = all 11 types); (4) the relationship to `weapon_styles` — greebles run **before** weapons in `generator.generate`'s assembly order (`scatter_greebles` first, then `scatter_weapons`), and both passes share the no-overwrite invariant via the per-cell `if shape_grid[x, y, z] == Role.EMPTY` gate at the call site so greebles are immune to weapon overwrites and weapons reliably anchor on top of the now-greebled silhouette; one Mermaid `flowchart LR` diagram mirrors the Shape-pipeline diagram's style (`generate_shape → _place_greebles bumps → mirror+connect+mirror → engine_style override → scatter_greebles → scatter_weapons → assign_roles`); cross-link footer points to `docs/cli.md` (`--list-greeble-types` / `--no-greebles` / `--greeble-density`) and `docs/web_ui.md` (`GET /api/greeble-types`); section spans 79 lines (under the 80-line cap), inserted immediately after the existing Shape-pipeline section's `assembly.py` (final pass) subsection and before the `## Related documentation` footer so the two pipeline sections sit visually adjacent at the same h2 depth; matches the Shape-pipeline section's heading depth (h2 / h3) and per-module subsection convention; pytest + ruff both green on docs-only change (closes feat-docs-architecture-greebles)
- feat(scripts): add `--csv` flag to `scripts/bench_palette.py` — emits a CSV document (`palette,mean_ms,p95_ms` header followed by one row per palette and a final `TOTAL` row) to stdout instead of the existing fixed-width per-palette table, useful for CI consumption and spreadsheet diffing of per-palette perf snapshots; mirrors the `feat-bench-summary-csv` pattern just shipped in `scripts/bench_summary.py` (same stdlib `csv.writer(sys.stdout, lineterminator="\n")` plumbing so future palette names containing commas / quotes / newlines are escaped correctly without us reinventing it, same `progress_stream = sys.stderr if args.csv else sys.stdout` hoist so the run-banner `bench_palette: palettes=... iterations=... seed=... ...` line goes to stderr in CSV mode and the stdout stream stays a clean parseable CSV document an operator can pipe straight into a spreadsheet / CI parser without filtering noise out, same default `False` so absent-flag behavior is byte-identical to the legacy fixed-width formatter — preserved verbatim by routing through the unchanged `print_table()` + `print_total()` pair); the new `print_csv(rows, all_samples_ms)` helper is a thin sibling of `print_table()` that re-uses the exact same per-iter aggregation (`all_samples_ms.mean()` and `np.percentile(all_samples_ms, 95)`) for the `TOTAL` row as the existing fixed-width formatter, so the two output paths can never drift on the summary numbers; argparse `--csv` (action `store_true`, default `False`) declared adjacent to the existing `--iterations` / `--limit` / `--seed` flags with help text `"Emit CSV (palette,mean_ms,p95_ms) instead of fixed-width table; useful for CI / spreadsheet ingest."`; new `tests/test_bench_smoke.py::test_bench_palette_csv_emits_csv` runs the script with `--csv --iterations 2 --limit 2 --seed 0` via `subprocess.run` and asserts exit 0 + first stdout line is exactly `palette,mean_ms,p95_ms` + at least one per-palette row appears in the CSV body + a `TOTAL,...` row appears in the body (catches a regression where the header emits but the body / TOTAL row is dropped); existing `tests/test_bench_smoke.py::test_bench_palette_runs_with_two_palettes_two_iterations` still asserts the fixed-width-table contract with `--csv` absent so the no-flag path is also pinned (closes feat-bench-palette-csv)
- feat(cli): add `--validate-palette PATH` flag — runs the strict palette-lint logic from `scripts/palette_lint.py::lint_palette` against a single palette YAML in-process and exits 0 (clean) or 1 (at least one error/warning); on a clean palette prints `OK` to stdout via the existing `_emit(args, ...)` funnel so `--quiet` silences it on the success path; on a dirty palette emits one `error: <msg>` line per error and one `warn: <msg>` line per warning to `sys.stderr` so diagnostics always surface (even paired with `--quiet`); lifts the strict-lint into the user-facing CLI so palette authors don't need to remember the helper script path (`.venv/Scripts/python scripts/palette_lint.py --file palettes/foo.yaml --strict`); the lint module isn't part of the installed package (it lives under `scripts/`), so the handler loads it lazily via `importlib.util.spec_from_file_location` from a path computed relative to the package root (`Path(__file__).resolve().parent.parent.parent / "scripts" / "palette_lint.py"`) — chosen over `sys.path` mutation as the safer pattern for a one-shot import; the loaded module is registered in `sys.modules` *before* `exec_module` so Python's `@dataclass` annotation resolution (which looks up `cls.__module__` in `sys.modules`) succeeds for the `LintResult` dataclass — without this, dataclass construction raises `AttributeError: 'NoneType' object has no attribute '__dict__'` (caught and fixed during initial implementation); argparse declaration `--validate-palette PATH` (`metavar="PATH"`, `type=pathlib.Path`) placed immediately after `--palette-info` in `build_parser` so the three palette-related flags (`--list-palettes`, `--palette-info`, `--validate-palette`) stay adjacent in the help-text grouping; short-circuit handler in `cli.main` placed immediately after the `--palette-info` handler for the same visual adjacency; defensive checks: missing target file → `--validate-palette: file not found: <path>` to stderr + exit 1; missing lint module → `--validate-palette: lint module not found at <path>` to stderr + exit 1; `LintResult` is treated as "clean" only when *both* `errors` and `warnings` are empty (strict mode — matches `scripts/palette_lint.py --strict`); two new tests in `tests/test_cli.py` cover (a) `test_cli_validate_palette_clean` runs against the strict-clean `palettes/sci_fi_industrial.yaml` (the canonical default palette) and asserts exit 0 + `OK` in stdout (without `--quiet`), and (b) `test_cli_validate_palette_dirty` writes a deliberately-broken palette YAML to `tmp_path` (every required role mapped to `"not a block"` — invalid block id format, no `namespace:id` colon and contains a space) and asserts exit 1 + at least one `error:`-prefixed diagnostic line on stderr (without pinning the exact wording, which is owned by `scripts/palette_lint.py::lint_palette`) (closes feat-cli-validate-palette)
- test(properties): add `StructureStyle` × seed-grid stability property test — new `tests/test_properties.py::test_property_structure_style_seed_grid_generates_non_empty_litematic` deterministically pins every `StructureStyle` enum member (6 members: FRIGATE, FIGHTER, DREADNOUGHT, SHUTTLE, HAMMERHEAD, CARRIER) crossed with the small fixed seed grid `[0, 1, 7]` (3 seeds — matches the hull/engine/wing/cockpit/greeble/weapon-type sibling parametrize tests), asserting `generate()` writes a `.litematic` file with `os.path.getsize(...) > 0` and `block_count > 0`; mirror of the wing-style and cockpit-style parametrize tests in `feat-tests-property-wing-styles` / `feat-tests-property-cockpit-styles`, threaded via `ShapeParams.structure_style` (constructed fresh per param pair) rather than a top-level `generate()` kwarg since `StructureStyle` is plumbed through `ShapeParams` rather than `generate()`'s public signature — completes deterministic every-member coverage for the last public shape-style enum so a regression in any single structure profile (silhouette taper / engine override / wing override / hull rx-ry scale in `src/spaceship_generator/structure_styles.py`) surfaces deterministically as a self-named failure node rather than relying on Hypothesis sampling; uses `pytest.mark.parametrize` (chosen over Hypothesis for the same reasons as the sibling tests — deterministic, faster, parametrize IDs self-name failures as `[seed-structure_style]`) with `ids=lambda s: s.value` so failure node IDs read e.g. `[7-frigate]` rather than the noisy `<StructureStyle.FRIGATE: 'frigate'>` repr; complements the existing Hypothesis-based `test_property_structure_x_hull_cross_product_no_crash` which samples 20 random `(StructureStyle, HullStyle)` pairs and may legitimately skip individual `StructureStyle` members on any given run — this new test instead asserts the seed-grid generation invariant (file written, non-zero bytes, block_count > 0) for every member at fixed seeds; placed immediately after the cockpit-style parametrize sibling and before the greeble-type comment block to keep the five shape-style siblings (hull / engine / wing / cockpit / structure) visually adjacent in the file; total of 18 new test nodes (6×3) run in ~1.0 s on the dev box at `length=16/width=8/height=6`; full `pytest -q` (2110 tests) + `ruff check .` both green (closes feat-tests-property-structure-styles)
- feat(scripts): add `--csv` flag to `scripts/bench_summary.py` — emits a CSV document (`bench,metric,iterations` header followed by one row per bench) to stdout instead of the existing fixed-width aggregate table, useful for CI consumption and spreadsheet diffing of perf snapshots; defaults to `False` so absent-flag behavior is byte-identical to the legacy fixed-width formatter (preserved verbatim by routing through the unchanged `print_table()` path); under the hood the new `print_csv()` helper writes through the stdlib `csv.writer(sys.stdout, lineterminator="\n")` so future bench names containing commas / quotes / newlines are escaped correctly without us reinventing it; in `--csv` mode the run-banner (`bench_summary: iterations=... seed=... ...`) and the per-bench `running <name> ...` progress lines are routed to stderr so the stdout stream stays a clean parseable CSV document an operator can pipe straight into a spreadsheet / CI parser without filtering noise out (in default fixed-width-table mode they stay on stdout where they've always been); failed benches in CSV mode emit a row with `metric=FAIL` and `iterations=0` matching the existing fixed-width FAIL handling so the row count stays stable across invocations and a downstream parser can detect failures by string match on the `FAIL` sentinel; argparse `--csv` (action `store_true`, default `False`) declared adjacent to the existing flags with help text `"Emit CSV (bench,metric,iterations) instead of fixed-width table; useful for CI / spreadsheet ingest."`; new `tests/test_bench_smoke.py::test_bench_summary_csv_emits_csv` runs the umbrella with `--csv --iterations 2 --limit 2 --fleet-count 2 --seed 0` via `subprocess.run` and asserts exit 0 + first stdout line is exactly `bench,metric,iterations` + at least one bench name appears in the subsequent rows (catches a regression where the header emits but the body is dropped); existing `tests/test_bench_smoke.py::test_bench_summary_runs_minimal` still asserts the fixed-width-table contract with `--csv` absent so the no-flag path is also pinned (closes feat-bench-summary-csv)
- feat(cli): add `--list-structure-styles` flag — prints every `StructureStyle` enum value on its own line in enum-declaration order, exits 0; mirrors the existing `--list-cockpit-styles` (`feat-cli-list-cockpit-styles`), `--list-greeble-types` (`feat-cli-list-greeble-types`), and `--list-weapon-types` (`feat-cli-list-weapon-types`) flags structurally — same no-header/no-indent output, same `_emit()` funnel so the `--quiet` carve-out keeps working without a special case (silenced when paired with `--quiet` like the other `--list-*` flags), same short-circuit pattern returning `0`; argparse declaration placed immediately after `--list-cockpit-styles` so the structural sibling `--list-*-styles` flags stay adjacent in `build_parser` and the help-text grouping reads as a sibling pair; handler iterates `StructureStyle` directly (StrEnum natural iteration = declaration order, deterministic and stable across runs) so callers can pipe straight into another tool without parsing grouped output; `StructureStyle` is already imported from `spaceship_generator.shape` (used by `--structure-style` choices and `--list-styles`-adjacent plumbing), so no new import was needed; new `tests/test_cli.py::test_cli_list_structure_styles` covers exit 0 + every member's `.value` present + deterministic enum-declaration order via `[line for line in lines if line] == [s.value for s in StructureStyle]` (no hard-coded list, so the test doesn't drift when a new structure style is added) + asserts none of the `--list-styles` group headers (`Hull styles:` / `Engine styles:` / `Wing styles:` / `Cockpit styles:` / `Weapon types:`) leak into the output (closes feat-cli-list-structure-styles)
- test(properties): add `CockpitStyle` × seed-grid stability property test — new `tests/test_properties.py::test_property_cockpit_style_seed_grid_generates_non_empty_litematic` deterministically pins every `CockpitStyle` enum member (6 members: BUBBLE, POINTED, INTEGRATED, CANOPY_DOME, WRAP_BRIDGE, OFFSET_TURRET) crossed with the small fixed seed grid `[0, 1, 7]` (3 seeds — matches the hull/engine/wing/greeble/weapon-type sibling parametrize tests), asserting `generate()` writes a `.litematic` file with `os.path.getsize(...) > 0` and `block_count > 0`; mirror of the wing-style parametrize test in `feat-tests-property-wing-styles`, but threaded via `ShapeParams.cockpit_style` (constructed fresh per param pair) rather than a top-level `generate()` kwarg since `CockpitStyle` is plumbed through `ShapeParams` rather than `generate()`'s public signature — completes deterministic every-member coverage for every public shape-style enum (hull / engine / wing / cockpit) so a regression in any single cockpit placer surfaces deterministically as a self-named failure node rather than relying on Hypothesis sampling; uses `pytest.mark.parametrize` (chosen over Hypothesis for the same reasons as the sibling tests — deterministic, faster, parametrize IDs self-name failures as `[seed-cockpit_style]`) with `ids=lambda c: c.value` so failure node IDs read e.g. `[7-bubble]` rather than the noisy `<CockpitStyle.BUBBLE: 'bubble'>` repr; complements the existing Hypothesis-based `test_property_cockpit_style_deterministic_per_seed` which asserts byte-equality across repeat calls but only over Hypothesis-sampled seeds — this new test instead asserts the seed-grid generation invariant (file written, non-zero bytes, block_count > 0) for every member at fixed seeds; placed immediately after the wing-style parametrize sibling and before the greeble-type comment block to keep the four shape-style siblings (hull / engine / wing / cockpit) visually adjacent in the file; total of 18 new test nodes (6×3) run in ~1.0 s on the dev box at `length=16/width=8/height=6`; full `pytest -q` (2091 tests) + `ruff check .` both green (closes feat-tests-property-cockpit-styles)
- docs(bench): add `docs/bench.md` — catalog of every `scripts/bench_*.py` micro-benchmark (9 scripts: `bench_compare`, `bench_fleet`, `bench_full_pipeline`, `bench_generator`, `bench_greeble_density`, `bench_mem`, `bench_palette`, `bench_shape`, `bench_summary`) with a one-line description and the canonical invocation per script; rendered as a 3-column Markdown table (`Script | What it measures | Example invocation`) matching the table style of `docs/palettes.md` / `docs/cli.md` / `docs/troubleshooting.md`; descriptions and example invocations are sourced from each script's module docstring + `argparse --help` output and the smoke-test invocation pattern in `tests/test_bench_smoke.py` so the doc stays in lockstep with the actual scripts; top-of-file cross-link header points to `quickstart.md` (5-minute walk), `cli.md` (CLI flag reference), `troubleshooting.md` (common errors), and `faq.md` ("how do I...?" reference) so an operator can hop between the four "operator-facing" docs in one click; "How to read the output" footer paragraph documents the shared `mean_ms` / `p95_ms` / `TOTAL` table convention used by every wall-clock bench, the `mean_mb` / `p95_mb` / `max_mb` substitution for `bench_mem.py`, and the cProfile-attributed `phase | total_s | mean_s | pct` shape for `bench_generator.py`; "Aggregate snapshot" footer documents `bench_summary.py` as the one-shot driver that runs every sibling bench in sequence and prints their TOTAL rows in a single consolidated table — the canonical "perf snapshot before/after a refactor" command; 42 lines total (well under the 100-line cap, no fenced code-block delimiters); one-line README link added immediately after the existing `docs/faq.md` link in the intro paragraph (no restructure — the line now reads "Wondering 'how do I...?' See [docs/faq.md](docs/faq.md) for the common-question reference. Profiling a refactor? See [docs/bench.md](docs/bench.md) for the bench-script catalog.") (closes feat-docs-bench)
- feat(cli): add `--list-shape-styles-json` flag — machine-readable variant of `--list-shape-styles` that emits a single JSON document `{"hull_styles":[...],"engine_styles":[...],"wing_styles":[...]}` to stdout in enum-declaration order, exits 0; mirrors the existing `--list-presets-json` flag (`feat-cli-list-presets-json`) structurally — same carve-out from `--quiet` (emits via `print(..., file=sys.stdout)` directly rather than the `_emit` funnel that gates on `--quiet`, documented in the help text), same mutual-exclusion guard against the human-formatted sibling (rejected via `parser.error` → exit 2 + `mutually exclusive` stderr message, mirroring the `--list-presets` vs `--list-presets-json` pattern); argparse declaration placed immediately after `--list-shape-styles` so the sibling pair stays adjacent in `build_parser`; payload shape is byte-identical to `GET /api/shape-styles` (same `[s.value for s in HullStyle]` etc. serialization and key set), so a CLI-driven discovery client and a web client see the exact same enumeration; three new tests in `tests/test_cli.py` cover (a) happy path produces a single parseable JSON document with keys `{"hull_styles","engine_styles","wing_styles"}` each matching the corresponding enum's `[s.value for s in ...]` declaration order exactly (no hard-coded list, so the test doesn't drift when a new style is added), (b) `--quiet --list-shape-styles-json` still emits the JSON document, and (c) `--list-shape-styles --list-shape-styles-json` errors non-zero with both flag names and `mutually exclusive` in stderr (closes feat-cli-list-shape-styles-json)
- feat(api): add `GET /api/structure-styles` endpoint — narrower JSON sibling of `/api/styles` returning only the `StructureStyle` enum values under a single `structure_styles` key in enum-declaration order; parallels the existing `/api/cockpit-styles` / `/api/greeble-types` / `/api/weapon-types` / `/api/shape-styles` pattern — clients that only need the structure picker can fetch this endpoint instead of pulling the larger `/api/styles` (which doesn't even include structure styles, since structures are not "shape" pickers in the `--list-shape-styles` sense) or `/api/meta` payloads; values emitted via `[s.value for s in StructureStyle]` so the serialization is byte-identical to the matching slice of `/api/meta`'s `structure_styles` array and stable across runs; `StructureStyle` is already imported from `...shape` at the top of `ship.py` (used by `index()` / `do_generate()` / `api_meta()` view functions), so no new import was needed; new `StructureStyles` schema in `_OPENAPI_COMPONENTS` (with `required: ["structure_styles"]`, mirroring the `CockpitStyles` / `GreebleTypes` / `WeaponTypes` siblings) added immediately after `CockpitStyles`, and `/api/structure-styles` path added to `_OPENAPI_PATHS` immediately after `/api/cockpit-styles` so `/api/spec` enumerates the route and `tests/test_api_spec_validate.py` stays green; two new tests in `tests/test_api.py` cover (a) `test_api_structure_styles_ok` — 200 + `application/json` content-type + non-empty array + every `StructureStyle.value` present + exact enum-declaration-order match, and (b) `test_api_structure_styles_listed_in_openapi_spec` — path appears in `/api/spec` with a `summary` and `200` response; one row added to the `/api/*` discovery table in `docs/web_ui.md` immediately after the `/api/cockpit-styles` row (closes feat-api-structure-styles)
- feat(scripts): add `scripts/bench_greeble_density.py` — per-greeble-density wall-clock micro-benchmark wrapping a full `generate()` call (shape + texture + weapons + `.litematic` write to a tmpdir) in `time.perf_counter()` over N iterations *for each greeble-density value* in a user-supplied (or default) sweep; argparse `--iterations N` (default 3), `--densities CSV` (default `0.0,0.25,0.5,0.75,1.0`, comma-separated floats in `[0.0, 1.0]` parsed via a custom `_parse_densities` argparse type that surfaces unparseable tokens / out-of-range values as a clean `error:` line on stderr rather than a stack trace), `--seed S` (default 0); palette pinned to `sci_fi_industrial` so the only varying axis is greeble density (matches `bench_full_pipeline.py`'s default palette so the two benches stay apples-to-apples on the palette dimension); ship footprint fixed at `length=16/width_max=8/height_max=6` via `ShapeParams(...)` so a 5-density × 3-iter sweep completes in seconds rather than minutes — the *slope* vs density is what this bench surfaces, not the absolute wall-clock at production-sized footprints; `greeble_density` is passed directly to `generate()` (which accepts `[0.0, 1.0]`) rather than via `ShapeParams.greeble_density` (which is capped at `0.5`) so the sweep can include the upper-bound `1.0` sample; warm-up iteration on the first density runs untimed so import-time caching and initial palette-load work do not skew the first sample (mirrors `bench_palette`/`bench_full_pipeline`/`bench_mem`); prints a fixed-width `density | mean_ms | p95_ms` table (column width auto-fits the formatted density label) plus a TOTAL summary row aggregating per-iter samples across the entire sweep so a regression in any single density also shows up in the sweep-wide p95; numpy + stdlib only (np.percentile for p95, no pandas/matplotlib); `tests/test_bench_smoke.py::test_bench_greeble_density_runs_minimal` runs the script with `--iterations 2 --densities 0.0,0.5 --seed 0` via `subprocess.run` and asserts exit 0 + presence of `density`/`mean_ms`/`p95_ms`/`TOTAL` in stdout; complements `bench_palette.py` (per-palette one-density) and `bench_full_pipeline.py` (one ship deeper N) by surfacing the cost slope vs greeble density — foundation for `shapes-*` perf work where the greeble scatter is a likely hot path (closes feat-bench-greeble-density)
- test(properties): add `WeaponType` × seed-grid stability property test — new `tests/test_properties.py::test_property_weapon_type_seed_grid_generates_non_empty_litematic` deterministically pins every `WeaponType` enum member (5 members: TURRET_LARGE, MISSILE_POD, LASER_LANCE, POINT_DEFENSE, PLASMA_CORE) crossed with the small fixed seed grid `[0, 1, 7]` (3 seeds — matches the hull/engine-style and greeble-type siblings in `feat-tests-property-shape-styles` / `feat-tests-property-greeble-types`), asserting `generate()` writes a `.litematic` file with `os.path.getsize(...) > 0` and `block_count > 0`; mirrors how the `--weapon-type TYPE` CLI flag plumbs into `generate()` (`[WeaponType(args.weapon_type)]` → `generate(..., weapon_types=[that_type])`) so the per-type CLI plumbing is exercised end-to-end per enum member; uses `pytest.mark.parametrize` (chosen over Hypothesis for the same reasons as the hull/engine/greeble companions — deterministic, faster, parametrize IDs self-name failures as `[seed-weapon_type]`) with `ids=lambda t: t.value` so failure node IDs read e.g. `[7-turret_large]` rather than the noisy `<WeaponType.TURRET_LARGE: 'turret_large'>` repr; `weapon_count=4` is set on `generate()` so the scatter actually fires and the restricted-type list has a chance to matter (the writer no-ops at `weapon_count=0`); total of 15 new test nodes (5×3) run in ~1.1 s on the dev box at `length=16/width=8/height=6`; complements the Hypothesis-based `test_property_weapon_count_scales_weapon_specific_roles` which samples weapon count but never restricts to a single `WeaponType`, so a regression in any single weapon builder now surfaces deterministically as a self-named failure node (closes feat-tests-property-weapon-types)
- feat(api): add `GET /api/cockpit-styles` endpoint — narrower JSON sibling of `/api/styles` returning only the `CockpitStyle` enum values under a single `cockpit_styles` key in enum-declaration order; parallels the existing `/api/shape-styles` / `/api/greeble-types` / `/api/weapon-types` pattern — clients that only need the cockpit picker can fetch this endpoint instead of pulling the larger `/api/styles` (which doesn't even include cockpit styles, since cockpits are not "shape" pickers in the `--list-shape-styles` sense) or `/api/meta` payloads; values emitted via `[c.value for c in CockpitStyle]` so the serialization is byte-identical to the matching slice of `/api/meta`'s `cockpit_styles` array and stable across runs; `CockpitStyle` is already imported from `...shape` at the top of `ship.py` (used by `index()` / `do_generate()` / `api_meta()` view functions), so no new import was needed; new `CockpitStyles` schema in `_OPENAPI_COMPONENTS` (with `required: ["cockpit_styles"]`, mirroring the `GreebleTypes` and `WeaponTypes` siblings) and `/api/cockpit-styles` path in `_OPENAPI_PATHS` immediately after `/api/greeble-types` so `/api/spec` enumerates the route and `tests/test_api_spec_validate.py` stays green; two new tests in `tests/test_api.py` cover (a) `test_api_cockpit_styles_ok` — 200 + `application/json` content-type + non-empty array + every `CockpitStyle.value` present + exact enum-declaration-order match, and (b) `test_api_cockpit_styles_listed_in_openapi_spec` — path appears in `/api/spec` with a `summary` and `200` response; one row added to the `/api/*` discovery table in `docs/web_ui.md` immediately after the `/api/weapon-types` row (closes feat-api-cockpit-styles)
- test(properties): add `WingStyle` × seed-grid stability property test — new `tests/test_properties.py::test_property_wing_style_seed_grid_generates_non_empty_litematic` deterministically pins every `WingStyle` enum member (6 members: STRAIGHT, SWEPT, DELTA, TAPERED, GULL, SPLIT) crossed with the small fixed seed grid `[0, 1, 7]` (3 seeds — matches the hull/engine/greeble-type sibling parametrize tests), asserting `generate()` writes a `.litematic` file with `os.path.getsize(...) > 0` and `block_count > 0`; mirror of the hull/engine-style parametrize tests in `feat-tests-property-shape-styles`, but threaded via `ShapeParams.wing_style` (constructed fresh per param pair) rather than a top-level `generate()` kwarg since `WingStyle` is plumbed through `ShapeParams` rather than `generate()`'s public signature — closes the explicit carve-out in the prior CHANGELOG note for the hull/engine sibling ("WingStyle is intentionally not duplicated here since it flows only via `ShapeParams`") so every public shape-style enum now has identical deterministic every-member coverage; uses `pytest.mark.parametrize` (chosen over Hypothesis for the same reasons as the sibling tests — deterministic, faster, parametrize IDs self-name failures as `[seed-wing_style]`) with `ids=lambda s: s.value` so failure node IDs read e.g. `[7-swept]` rather than the noisy `<WingStyle.SWEPT: 'swept'>` repr; covers a regression in any single wing placer (`_place_straight` / `_place_swept` / `_place_delta` / `_place_tapered` / `_place_gull` / `_place_split` in `src/spaceship_generator/wing_styles.py`) one tick earlier than the Hypothesis-sampled `test_property_all_style_combos_symmetric` would since this explicitly visits every member rather than sampling; total of 18 new test nodes (6×3) run in ~0.9 s on the dev box at `length=16/width=8/height=6` (closes feat-tests-property-wing-styles)
- feat(cli): add `--list-cockpit-styles` flag — prints every `CockpitStyle` enum value on its own line in enum-declaration order, exits 0; mirrors the existing `--list-greeble-types` (`feat-cli-list-greeble-types`) and `--list-weapon-types` (`feat-cli-list-weapon-types`) flags structurally — same no-header/no-indent output, same `_emit()` funnel so the `--quiet` carve-out keeps working without a special case (silenced when paired with `--quiet` like the other `--list-*` flags), same short-circuit pattern returning `0`; argparse declaration placed immediately after `--list-weapon-types` so the structural sibling `--list-*-types` / `--list-*-styles` flags stay adjacent in `build_parser` and the help-text grouping reads as a sibling triplet; handler iterates `CockpitStyle` directly (StrEnum natural iteration = declaration order, deterministic and stable across runs) so callers can pipe straight into another tool without parsing grouped output; `CockpitStyle` is already imported from `spaceship_generator.shape` (used by `--cockpit` choices), so no new import was needed; new `tests/test_cli.py::test_cli_list_cockpit_styles` covers exit 0 + every member's `.value` present + deterministic enum-declaration order via `[line for line in lines if line] == [c.value for c in CockpitStyle]` (no hard-coded list, so the test doesn't drift when a new cockpit style is added) + asserts none of the `--list-styles` group headers (`Hull styles:` / `Engine styles:` / `Wing styles:` / `Cockpit styles:` / `Weapon types:`) leak into the output (closes feat-cli-list-cockpit-styles)
- feat(cli): add `--list-presets-json` flag — machine-readable variant of `--list-presets` that emits a single JSON array of preset entries to stdout, one document per call, alphabetical by name (matches `list_presets()`); each entry is `{"name": ..., "description": ..., ...}` and includes every public top-level field from the `SHIP_PRESETS` dict (description, hull_style, engine_style, wing_style, cockpit_style, greeble_density, weapon_count, weapon_types, size — all StrEnums serialize as strings, tuples as lists), with private `_*` keys defensively skipped (none today, but mirrors the brief's "every public field" contract); mutually exclusive with `--list-presets` (rejected via `parser.error` → exit 2 + `mutually exclusive` stderr message, mirroring the `--stats` vs `--stats-json` pattern); deliberately NOT silenced by `--quiet` so scripts can pair `--quiet --list-presets-json` (same carve-out as `--stats-json` and `--output-json`, documented in the help text — emits via `print(..., file=sys.stdout)` directly rather than the `_emit` funnel that gates on `--quiet`); defensive fallback path for the `_presets is None` partial-rollout case still emits a valid JSON document (empty array) on stdout so downstream parsers don't fault, with the breadcrumb on stderr; three new tests in `tests/test_cli.py` cover (a) happy path produces a single parseable JSON array with `len(list_presets())` entries each carrying `name` + `description` keys in alphabetical order, (b) `--quiet --list-presets-json` still emits the JSON document, and (c) `--list-presets --list-presets-json` errors non-zero with both flag names and `mutually exclusive` in stderr (closes feat-cli-list-presets-json)
- feat(api): add `GET /api/greeble-types` endpoint — narrower JSON sibling of `/api/styles` returning only the `GreebleType` enum values under a single `greeble_types` key, mirroring the CLI `--list-greeble-types` flag (`feat-cli-list-greeble-types`); enum values emitted in enum-declaration order via the same `[t.value for t in GreebleType]` serialization used by `/api/styles` so the two endpoints stay byte-identical for the shared key (asserted by the new `test_api_greeble_types_ok` order check); new `GreebleTypes` schema in `_OPENAPI_COMPONENTS` and `/api/greeble-types` path in `_OPENAPI_PATHS` so `/api/spec` enumerates the route and `tests/test_api_spec_validate.py` stays green; two new tests in `tests/test_api.py` cover 200 + content-type + non-empty array + every `GreebleType.value` present in declaration order, and presence in `/api/spec`'s `paths` dict; one row added to the `/api/*` discovery table in `docs/web_ui.md` (closes feat-api-greeble-types)
- feat(cli): add `--list-weapon-types` flag — prints every `WeaponType` enum value on its own line in enum-declaration order, exits 0; mirrors the existing `--list-greeble-types` flag (`feat-cli-list-greeble-types`) structurally — same no-header/no-indent output, same `_emit()` funnel so the `--quiet` carve-out keeps working without a special case, same short-circuit pattern; replaces the older `--list-weapon-types` handler that emitted indented `  weapon_name` lines and called `raise SystemExit(0)` instead of `return 0` — the new handler drops the indent (callers can now pipe straight into another tool without stripping leading whitespace) and uses the same `return 0` exit pattern as every other `--list-*` short-circuit; argparse declaration moved up to sit immediately after `--list-greeble-types` (was previously orphaned under `--list-presets`); when the optional `weapon_styles` module is unavailable the handler still prints the `weapon_styles unavailable: <reason>` fallback to stderr and returns exit 1 (preserved from the old handler); new `tests/test_cli.py::test_cli_list_weapon_types` covers exit 0 + every member's `.value` present + deterministic enum-declaration order via `[line for line in lines if line] == [w.value for w in WeaponType]` (no hard-coded list, so the test doesn't drift when a new weapon type is added) + asserts none of the `--list-styles` group headers leak into the output (closes feat-cli-list-weapon-types)
- docs(troubleshooting): add `docs/troubleshooting.md` — common-error reference companion to `docs/quickstart.md` covering palette-not-found (`Palette 'NAME' not found at .../palettes/NAME.yaml` from `palette.load_palette`), `argparse` `invalid choice:` errors for `--hull-style` / `--engine-style` / `--wing-style` / `--cockpit` / `--greeble-style` / `--weapon-type` (each cross-linked to the corresponding `--list-shape-styles` / `--list-greeble-types` / `--list-weapon-types` discovery flag), the four `parser.error` mutually-exclusive-flag pairs sourced verbatim from `cli.py` (`--no-weapons` vs `--weapon-count`, `--no-greebles` vs `--greeble-density`, `--stats` vs `--stats-json`, `--output -` vs `--repeat`/`--fleet-count`/`--seeds`), the `--ship-size W>=4 H>=4 L>=8` floor and `--greeble-density [0.0, 1.0]` range from the `argparse.ArgumentTypeError` raisers, the three `--seeds` parse modes (single, range, mixed) with their canonical error text, the web `HTTP 429 {"error": "rate_limited"}` path with the `SHIPFORGE_RATE_LIMIT` (default 30) / `SHIPFORGE_RATE_WINDOW` (default 60s) env tunables read from `web/blueprints/ratelimit.py`, and the optional-dep `ModuleNotFoundError` paths (`flask`, `jsonschema`, `Pillow`) with the `pip install -e .[dev]` / `requirements-dev.txt` fixes plus the `presets unavailable` / `weapon_styles unavailable` stderr-warning fallbacks from `cli.py`'s try/except `ImportError` guards; rendered as a single 14-row Markdown table (Symptom | Cause | Fix) followed by a "Where to find more" footer cross-linking `quickstart.md`/`cli.md`/`palettes.md`/`web_ui.md`; 29 lines total (well under the 80-line cap, no fenced code-block delimiters); every error message quoted verbatim from `parser.error(...)` / `argparse.ArgumentTypeError` / `raise FileNotFoundError(...)` call sites in `src/spaceship_generator/cli.py` and `src/spaceship_generator/palette.py` so the doc and the actual user-facing stderr stay in lockstep; one-line link added to README's existing intro paragraph immediately after the `docs/quickstart.md` link (no restructure — the line now reads "New here? ... Hit an error? See [docs/troubleshooting.md](docs/troubleshooting.md) for common failures and one-line fixes.") (closes feat-docs-troubleshooting)
- feat(api): add `GET /api/weapon-types` endpoint — narrower JSON sibling of `/api/styles` returning only the `WeaponType` enum values under a single `weapon_types` key in enum-declaration order; parallels the `/api/shape-styles` / `--list-shape-styles` pattern — clients that only need the weapon archetype picker can fetch this endpoint instead of pulling the larger `/api/styles` payload that also includes hull/engine/wing + greeble types; values are emitted via `[t.value for t in WeaponType]` so the serialization is byte-identical to the matching slice of `/api/styles` and stable across runs; new `WeaponTypes` schema in `_OPENAPI_COMPONENTS` (with `required: ["weapon_types"]`) and `/api/weapon-types` path in `_OPENAPI_PATHS` so `/api/spec` enumerates the route and `tests/test_api_spec_validate.py` stays green; two new tests in `tests/test_api.py` cover (a) `test_api_weapon_types_ok` — 200 + `application/json` content-type + non-empty array + every `WeaponType.value` present + exact enum-declaration-order match, and (b) `test_api_weapon_types_listed_in_openapi_spec` — path appears in `/api/spec` with a `summary` and `200` response; one row added to the `/api/*` discovery table in `docs/web_ui.md` (closes feat-api-weapon-types)
- feat(palettes): add `deep_dark`, `jagged_peaks` biome palettes — biome pack rounding the catalog to 57; `deep_dark` (sculk hull, deepslate HULL_DARK accent, cyan-stained-glass windows, deepslate-tile engines with soul-lantern engine glow, tinted-glass cockpit, cobbled-deepslate wings, chiseled-deepslate greebles, soul-torch running lights, polished-deepslate interior — Minecraft 1.19 deep dark sculk biome theme; chose `soul_lantern` for ENGINE_GLOW because `sculk_catalyst` is not in the strict-lint known-emissive list — `ancient_city` already ships with that warning, so `deep_dark` deliberately diverges to stay strict-clean) and `jagged_peaks` (snow-block hull, blue-ice HULL_DARK accent, light-blue-stained-glass windows, polished-diorite engines with sea-lantern engine glow, glass cockpit, powder-snow wings, stone greebles, lantern running lights, gravel interior — Minecraft 1.18 snowy mountain peaks biome theme; chose `blue_ice` over `packed_ice` for HULL_DARK because the canonical packed-ice preview hex is too close to snow-block to clear the 1.5 contrast floor, and chose distinct `lantern` for LIGHT vs `sea_lantern` for ENGINE_GLOW so every role maps to a distinct block id and no duplicate-mapping warnings fire); both pass strict palette lint (WINDOW luminance ≥ 0.35, HULL/HULL_DARK contrast ≥ 1.5, ENGINE_GLOW emissive) with zero warnings; catalog rows added to `docs/palettes.md` in alphabetical order (`deep_dark` between `dark_forest`/`deepslate_drone`, `jagged_peaks` between `ice_spikes`/`jungle_canopy`) and the header count bumped from 55 to 57 (closes feat-palettes-biome-pack-2026-04-30)
- feat(palettes): add `swamp`, `dark_forest` biome palettes — biome pack rounding the catalog to 55; `swamp` (mossy-cobblestone hull, oak-log HULL_DARK accent, lime-stained-glass windows, stripped-oak-log engines with ochre-froglight engine glow, tinted-glass cockpit, oak-leaves vine-canopy wings, stripped-oak-wood greebles, lantern running lights, moss-block interior — Minecraft swamp biome theme) and `dark_forest` (dark-oak-log hull, dark-oak-planks HULL_DARK accent, yellow-stained-glass canopy-shaft windows, stripped-dark-oak-log engines with shroomlight engine glow, tinted-glass cockpit, brown-mushroom-block wings, stripped-dark-oak-wood greebles, lantern running lights, moss-block interior — Minecraft dark forest / roofed forest biome theme); both pass strict palette lint (WINDOW luminance ≥ 0.35, HULL/HULL_DARK contrast ≥ 1.5, ENGINE_GLOW emissive) with zero warnings (every role maps to a distinct block id so no duplicate-mapping warnings); catalog rows added to `docs/palettes.md` in alphabetical order (`dark_forest` between `cyberpunk_neon`/`deepslate_drone`, `swamp` between `steampunk_brass`/`swamp_bog`) and the header count bumped from 53 to 55 (closes feat-palettes-biome-pack-2026-04-29b)
- feat(cli): add `--list-greeble-types` flag — prints every `GreebleType` enum value on its own line in enum-declaration order, exits 0; narrower sibling of `--list-styles` (which also includes hull/engine/wing/cockpit/weapon types) and `--list-shape-styles` (commit `c9aefd8`); pins just the greeble-type catalog so callers can grep/diff the available `--greeble-style TYPE` choices without parsing grouped output; no header/indent prefix (only one enum to emit, so the indent-by-two grouping used by `--list-shape-styles` would be noise) — output is the bare value list, suitable for piping straight into another tool; `--quiet` carve-out unchanged (suppresses stdout same as the other `--list-*` flags via the existing `_emit()` funnel); existing `--list-styles` / `--list-shape-styles` behavior unchanged; new `tests/test_cli.py::test_cli_list_greeble_types` covers exit 0, every member's `.value` present, and deterministic enum-declaration order via direct list comparison; full `pytest -q` (1974 tests) + `ruff check .` both green (closes feat-cli-list-greeble-types)
- feat(scripts): add `scripts/bench_summary.py` — umbrella driver that runs every sibling bench script (`bench_shape.py`, `bench_full_pipeline.py`, `bench_palette.py`, `bench_mem.py`, `bench_fleet.py`) via `subprocess.run([sys.executable, ...])` (so the active interpreter / venv is shared with the children) and prints a single fixed-width aggregate table; argparse `--iterations N` (default 2), `--limit N` (default 2, forwarded to `bench_palette --limit`), `--seed S` (default 0), `--fleet-count N` (default 2, forwarded to `bench_fleet --fleet-count`) — defaults chosen to match the smoke-test footprint so an interactive run takes seconds, not minutes; each child bench is invoked with a centrally-built argv tail (a per-bench `*_args` method on `_ArgsBuilder`) so the bench-spec table stays declarative and adding a new bench is one `BenchSpec` entry plus one args method; per-bench TOTAL line is parsed by a single shared regex (the five sibling benches all print `TOTAL  mean_X  p95_X  [...]` in the same fixed-width template, so the first numeric field after `TOTAL` is always the mean) and surfaced as the headline metric in the aggregate table — labelled `ms` for the four wall-clock benches and `mb` for `bench_mem`'s peak Python heap; columns are `bench | metric | iterations` with the bench-name column auto-fit to the longest row label; if any single child bench fails (non-zero exit, missing `TOTAL` line, unparseable mean) the row prints `FAIL` in the metric column rather than aborting the whole run, the captured stderr is printed below the table, and the umbrella exits 1 only at the end (so a single broken bench doesn't blackhole the rest of the snapshot); `tests/test_bench_smoke.py::test_bench_summary_runs_minimal` runs the umbrella with `--iterations 2 --limit 2 --fleet-count 2 --seed 0` via `subprocess.run` and asserts exit 0 + every child bench script's name appears in stdout (catches a regression where the umbrella silently drops a row); stdlib only (no extra deps); turns the five micro-benches into a single "perf snapshot" command useful before/after a refactor — complements per-script depth with cross-script breadth (closes feat-bench-summary)
- test(cli): pin `--help` references every declared flag — new `tests/test_cli.py::test_cli_help_text_mentions_every_declared_flag` snapshot-style guard walks `build_parser()._actions` and asserts every flag string in each action's `option_strings` (long form, short alias, etc.) appears verbatim in `parser.format_help()`; the discovery is dynamic — adding a new `add_argument` to `cli.py` automatically widens the assertion without touching the test, so the test never goes stale and the failure mode it catches (silent removal/rename of a flag, or a `help=argparse.SUPPRESS` slip) surfaces immediately with the offending flag name(s) listed in the assertion message rather than as a vague help-text-changed error; also asserts `format_help()` returns a non-empty string and mentions the package name (`spaceship`) so a parser that's been gutted entirely also fails fast; complements the existing per-flag help assertions (e.g. `test_cli_no_weapons_help_mentions_both_flags`) by giving catalog-wide coverage of every flag in one cheap defensive test (~0.5 s); full `pytest -q` (1973 tests) + `ruff check .` both green (closes feat-tests-cli-help-snapshot)
- feat(palettes): add `desert_temple`, `nether_wastes` biome palettes — biome pack rounding the catalog to 53; `desert_temple` (sandstone hull, chiseled-sandstone HULL_DARK accent, orange-stained-glass windows, polished-granite engines with torch engine glow, yellow-stained-glass cockpit, smooth-sandstone wings, cut-sandstone greebles, redstone-lamp running lights, orange-terracotta interior — Minecraft desert pyramid temple theme) and `nether_wastes` (netherrack hull, nether-bricks HULL_DARK accent, red-stained-glass windows, magma-block engines with glowstone engine glow, tinted-glass cockpit, red-nether-bricks wings, basalt greebles, soul-torch running lights, blackstone interior — Minecraft nether wastes biome theme); both pass strict palette lint (WINDOW luminance ≥ 0.35, HULL/HULL_DARK contrast ≥ 1.5, ENGINE_GLOW emissive) with zero warnings; catalog rows added to `docs/palettes.md` in alphabetical order and the header count bumped from 51 to 53 (closes feat-palettes-biome-pack-2026-04-29)
- feat(scripts): add `scripts/bench_fleet.py` — fleet-build wall-clock micro-benchmark wrapping the public Python fleet-builder API (`spaceship_generator.fleet.generate_fleet` for the plan + `spaceship_generator.generator.generate` per planned ship) in `time.perf_counter()` over N iterations; argparse `--fleet-count N` (default 4), `--iterations N` (default 3), `--seed S`, `--palette NAME` mirroring `bench_full_pipeline.py`/`bench_palette.py`; planning + every per-ship `generate()` call are timed inside a single `perf_counter` window per sample so the timing reflects the whole fleet code path; per-ship parameter threading mirrors `cli._run_fleet_ship` (planned dims/wing → fresh `ShapeParams`, hull/engine styles + greeble density forwarded as `generate()` kwargs); deliberately calls the in-process Python API rather than shelling out to `python -m spaceship_generator --fleet-count N` so the sample doesn't include interpreter startup; prints a fixed-width `per_ship | fleet | TOTAL` table with `mean_ms` and `p95_ms` columns (per-ship row is `fleet_total_ms / fleet_count`, the average per-ship cost — not a per-ship distribution); numpy + stdlib only; warm-up iteration runs untimed so import-time caching and palette-load work don't skew the first sample (mirrors `bench_full_pipeline`/`bench_palette`/`bench_mem`); `tests/test_bench_smoke.py::test_bench_fleet_runs_with_two_ships_two_iterations` runs the script with `--fleet-count 2 --iterations 2 --seed 0` via `subprocess.run` and asserts exit 0 + presence of `stage`/`mean_ms`/`p95_ms`/`per_ship`/`fleet`/`TOTAL` in stdout; complements `bench_full_pipeline.py` (one ship, deeper N) and `bench_palette.py` (per-palette one-ship) by surfacing the cost of the multi-ship fleet path which the CLI exercises via `--fleet-count` (closes feat-bench-fleet)
- test(properties): add `GreebleType` × seed-grid stability property test — new `tests/test_properties.py::test_property_greeble_type_seed_grid_generates_non_empty_litematic` deterministically pins every `GreebleType` enum member (11 members: TURRET, DISH, VENT, ANTENNA, PANEL_LINE, SENSOR_POD, CIRCUIT_BOARD, BATTLE_DAMAGE, PIPE_CLUSTER, ORGANIC_GROWTH, NANO_MESH) crossed with the small fixed seed grid `[0, 1, 7]` (3 seeds — matches the hull/engine-style sibling in `3dbeea9`), asserting `generate()` writes a `.litematic` file with `os.path.getsize(...) > 0` and `block_count > 0`; mirrors how the `--greeble-style TYPE` CLI flag plumbs into `generate()` (`cli.py:687-691` → `[GreebleType(args.greeble_style)]` → `generate(..., greeble_types=[that_type])`) so the per-type CLI plumbing is exercised end-to-end per enum member; uses `pytest.mark.parametrize` (chosen over Hypothesis for the same reasons as the hull/engine companions — deterministic, faster, parametrize IDs self-name failures as `[seed-greeble_type]`) with `ids=lambda t: t.value` so failure node IDs read e.g. `[7-circuit_board]` rather than the noisy `<GreebleType.CIRCUIT_BOARD: 'circuit_board'>` repr; `greeble_density=0.3` is set on `ShapeParams` so the scatter actually fires and the restricted-type list has a chance to matter; total of 33 new test nodes (11×3) run in ~0.7 s on the dev box at `length=16/width=8/height=6`; complements the Hypothesis-based `test_property_hull_x_engine_matrix_produces_valid_grid` and the existing hull/engine parametrize tests, so a regression in any single greeble builder now surfaces deterministically as a self-named failure node (closes feat-tests-property-greeble-types)
- docs(quickstart): add `docs/quickstart.md` — 5-minute getting-started guide bridging the README intro and the dense `docs/cli.md` reference; walks through, in order, install (from the README's existing block) → first ship via `spaceship-generator --seed 42` → palette swap (`--palette NAME` + link to `docs/palettes.md`) → preset use (`--preset NAME` + link to `docs/presets.md`) → web UI launch via `flask --app spaceship_generator.web.app run` (link to `docs/web_ui.md`); 55 lines excluding code-fence delimiters (well under the 80-line cap); every command is copy-pasteable and sourced from existing `docs/cli.md` flag declarations so the doc and the CLI stay in lockstep; "Next steps" footer points to `cli.md`, `architecture.md`, and `palette_authoring.md` for deeper reading; one-line link added directly under the README intro paragraph (above the existing `## Pipeline` heading) so first-time visitors hit the guide without scrolling past the install + key-flags sections; full `pytest -q` (1971 tests) + `ruff check .` both green (docs-only change) (closes feat-docs-quickstart)
- feat(cli): add `--stats-json` flag — machine-readable variant of `--stats` that prints a single JSON document (block counts, dims, role tallies) to stdout instead of the human-formatted table; mutually exclusive with `--stats` (rejected via `parser.error` → exit 2 + stderr message, mirroring the `--no-greebles` vs `--greeble-density` pattern); deliberately NOT silenced by `--quiet` so scripts can pair `--quiet --stats-json` (same carve-out as `--output-json`, documented in the help text); under the hood `_print_stats` and the new `_print_stats_json` both consume a shared `_compute_stats(result)` helper that returns a dict with `seed`, `palette`, `shape`, `total_blocks`, `density`, `total_cells`, and a `roles` array of `{role, count, pct}` entries (sorted by count desc, EMPTY skipped) so the human and JSON paths can never drift; in bulk runs (`--seeds` / `--repeat` / `--fleet-count`) the result is one JSON document per ship, newline-delimited (NDJSON), parallelling how `--output-json` behaves; three new tests in `tests/test_cli.py` cover (a) happy path produces a single parseable JSON document with the expected keys + sorted-desc role counts + summed counts equal to `total_blocks`, (b) `--quiet --stats-json` still emits exactly one JSON line on stdout (no `Role distribution:` header, no `Seed:` success lines), and (c) `--stats --stats-json` errors non-zero with `mutually exclusive` in stderr (closes feat-cli-stats-json)
- docs(presets): add `docs/presets.md` catalog — rewrites the older partial-archetype-table doc into a full catalog of all 9 shipped presets (`battlecruiser`, `capital_carrier`, `corvette`, `dropship`, `freighter_heavy`, `gunship`, `interceptor`, `science_vessel`, `scout`) in a 2-column Markdown table sourced directly from each preset's `description:` field in `SHIP_PRESETS` (`src/spaceship_generator/presets.py`), alphabetical by preset name; matches the table style of `docs/palettes.md` (commit `36da455`) for visual consistency — the prior doc shipped only 6 of 9 archetypes (missing `scout`/`battlecruiser`/`capital_carrier`) and predates the per-preset `description:` field, so the new table is also the canonical mirror of `--list-presets` CLI output; companion sections preserved (Python usage with `apply_preset(...)` / `list_presets()`, raw-table inspection via `SHIP_PRESETS` + `PRESET_KEYS`, "Adding a new preset" checklist updated to require a `description:` line and alphabetical-by-name placement); one-line link added to the existing `--preset` bullet in README's `### Key flags` (no restructure) (closes feat-docs-presets)
- test(properties): add `HullStyle` × seed-grid stability property test — new `tests/test_properties.py::test_property_hull_style_seed_grid_generates_non_empty_litematic` deterministically pins every `HullStyle` enum member (10 members: ARROW, SAUCER, WHALE, DAGGER, BLOCKY_FREIGHTER, ORGANIC_BIO, HEXAGONAL_LATTICE, ASYMMETRIC_SCAVENGER, MODULAR_BLOCK, SLEEK_RACING) crossed with the small fixed seed grid `[0, 1, 7]` (3 seeds — keeps total runtime under the 30 s acceptance budget), asserting `generate()` writes a `.litematic` file with `os.path.getsize(...) > 0` and `block_count > 0`; companion `test_property_engine_style_seed_grid_generates_non_empty_litematic` adds the same coverage for every `EngineStyle` member (9 members) since `EngineStyle` is the other shape-style enum exposed directly on `generate()`'s public signature (`engine_style=`); `WingStyle` is intentionally not duplicated here since it flows only via `ShapeParams` and is already exercised by the Hypothesis `test_property_all_style_combos_symmetric` test plus the `StructureStyle × HullStyle` cross-product test; uses `pytest.mark.parametrize` (chosen over Hypothesis for the same reasons as the palette companion in `3321b88` — deterministic, faster, parametrize IDs self-name failures as `[seed-style]`) with `ids=lambda s: s.value` so failure node IDs read e.g. `[7-blocky_freighter]` rather than the noisy `<HullStyle.BLOCKY_FREIGHTER: 'blocky_freighter'>` repr; total of 57 new test nodes (10×3 + 9×3 = 30+27) run in ~1.2 s on the dev box at `length=16/width=8/height=6`; complements the Hypothesis-based `test_property_hull_x_engine_matrix_produces_valid_grid` which samples 20 random pairs and may legitimately skip enum members on any given run, so a regression in any single style now surfaces deterministically as a self-named failure node (closes feat-tests-property-shape-styles)
- feat(scripts): add `scripts/bench_palette.py` — per-palette wall-clock micro-benchmark wrapping a full `generate()` call (shape + texture + weapons + `.litematic` write to a tmpdir) in `time.perf_counter()` over N iterations *for each shipped palette*; argparse `--iterations N` (default 3), `--limit N` (default 0 = all palettes), `--seed S` mirroring `bench_full_pipeline.py`/`bench_mem.py`; palettes discovered dynamically via `spaceship_generator.palette.list_palettes()` (the same enumeration `tests/test_palette_lint.py` and `tests/test_properties.py` use), so adding a new YAML under `palettes/` automatically widens the matrix without touching this script; prints a fixed-width `palette | mean_ms | p95_ms` table (column width auto-fits the longest palette name) plus a TOTAL summary row aggregating per-iter samples across the entire catalog so a regression in any single palette also shows up in the catalog-wide p95; numpy + stdlib only (no pandas/matplotlib); `tests/test_bench_smoke.py::test_bench_palette_runs_with_two_palettes_two_iterations` runs the script with `--limit 2 --iterations 2 --seed 0` via `subprocess.run` and asserts exit 0 + presence of `palette`/`mean_ms`/`p95_ms`/`TOTAL` in stdout; complements `bench_full_pipeline.py` (one palette, deeper N) by surfacing per-palette cost variance — foundation for catching palette-driven slow paths early (closes feat-bench-palette)
- feat(cli): add `--no-weapons` shortcut — equivalent to `--weapon-count 0` (disables weapon scatter entirely); mutually exclusive with `--weapon-count` (errors with non-zero exit via `parser.error()` if both flags are passed); parallels the existing `--no-greebles` / `--greeble-density` mutual-exclusion pattern from `e33a3f2`; because `--weapon-count` defaults to `0`, the mutex check uses the `_explicit_flags(argv)` set (mirrors how `--from-manifest` distinguishes "user typed `--seed`" from "argparse filled in the default") rather than a `None`-sentinel comparison; downstream plumbing reads only `args.weapon_count` so no special-case threading is needed; three new tests in `tests/test_cli.py` cover (a) end-to-end resolution by asserting `--no-weapons` produces the same `--output-json` block count as `--weapon-count 0` while a `--weapon-count 5` run produces strictly more (sanity-checks that weapon scatter would have fired absent the shortcut), (b) the mutual-exclusion exit-non-zero + stderr message, and (c) the help text mentions both `--no-weapons` and `--weapon-count`; tiny convenience flag for callers who want a quick "no guns" toggle without remembering the integer flag (closes feat-cli-no-weapons)
- docs(web): add `docs/web_ui.md` — endpoint + UI reference catalog covering every HTML page served by the Flask blueprints (`/`, `POST /generate`, `/result/<gen_id>`) plus every binary/JSON auxiliary route used by the templates (`/preview/<gen_id>.png`, `/preview-lite`, `/voxels/<gen_id>.json`, `/block-texture/<id>.png`, `/download/<gen_id>`, `/download-fleet`) and every public `/api/*` route sourced from `_OPENAPI_PATHS` in `src/spaceship_generator/web/blueprints/ship.py` (palettes, palette detail, styles, shape-styles, presets, preset detail, meta, health, random, spec, generate, batch, result, fleet/plan, compare); each route documents method + path + summary + query/path/body params + success-response shape; also documents the rate-limited endpoints and the `SHIPFORGE_RATE_LIMIT` / `SHIPFORGE_RATE_WINDOW` / `SHIPFORGE_CSP` env tunables; one-line link added under the existing `## Usage — Web UI` section of `README.md` (no restructure); 4-column Markdown-table style matches `docs/cli.md` and `docs/palettes.md` for consistency (closes feat-docs-web-ui)
- feat(cli): add `--output -` flag — streams the raw `.litematic` bytes to `sys.stdout.buffer` instead of writing a file, enabling shell pipelines like `spaceship-generator --output - --seed 1 > ship.litematic` (or piping directly into a downstream tool such as `mc-server-tool import-schematic`); single-ship only and mutually exclusive with `--repeat`, `--fleet-count`, and `--seeds` (rejected via `parser.error` → exit 2 + stderr message); under the hood the generator writes into a `tempfile.TemporaryDirectory` and the bytes are read back and emitted on `sys.stdout.buffer.write` so the payload survives binary round-trip; success-path informational stdout lines (Seed/Palette/Wrote/...) are suppressed unconditionally so the binary stream isn't corrupted (regardless of `--quiet`); errors and warnings still flow through stderr; three new tests in `tests/test_cli.py` cover the happy path (non-empty bytes + gzip-magic `\x1f\x8b` prefix verifying the litematic NBT-in-gzip envelope, captured via `capfdbinary`) and the two conflict paths (`--output - --repeat 2` and `--output - --fleet-count 2` both exit non-zero with the offending flag named in stderr) (closes feat-cli-stdout-litematic)
- test(properties): add palette × seed-grid stability property test — new `tests/test_properties.py::test_property_palette_seed_grid_generates_non_empty_litematic` enumerates every palette in `palettes/*.yaml` dynamically (matches the `tests/test_palette_lint.py` discovery style — adding a YAML auto-widens the matrix) and crosses it with a small fixed seed grid `[0, 1, 7, 42, 99]`, asserting `generate()` writes a `.litematic` file with `os.path.getsize(...) > 0` and `block_count > 0`; uses `pytest.mark.parametrize` so failure node IDs self-name as `[palette-seed]` and an explicit `pytest.fail(f"...palette={...} seed={...}")` makes the offending pair obvious in either the parametrize ID or the message; runs ~255 generate() calls (51 palettes × 5 seeds) in ~2.2 s on the dev box at `length=16/width=8/height=6` (well under the 60 s acceptance budget — no `slow` marker needed); would have caught palette-driven regressions one tick earlier than pure shape-property tests (closes feat-tests-property-palette-stability)
- feat(palettes): add `windswept_hills`, `ice_spikes` palettes — biome pack for Minecraft 1.18 windswept hills mountains variant (stone hull, gravel dark accent, light-blue-stained-glass windows, smooth-stone engines, lantern engine glow, glass cockpit, spruce-plank wings, andesite greebles, lantern lights, stone-bricks interior) and the rare ice spikes cold biome (packed-ice hull, blue-ice dark accent, light-blue-stained-glass windows, prismarine-brick engines, sea-lantern engine glow, white-stained-glass cockpit, snow-block wings, dripstone-block greebles, sea-lantern lights, smooth-quartz interior); both pass strict palette lint (WINDOW luminance, HULL/HULL_DARK contrast, ENGINE_GLOW emissive); rounds palette count to 51; catalog rows added to `docs/palettes.md` in alphabetical order (closes feat-palettes-biome-pack-2026-04-28b)
- feat(cli): add `--version`/`-V` flag — prints `spaceship_generator <version>` to stdout and exits 0 via argparse's `version` action; version sourced from `spaceship_generator.__version__` (matches the `/api/health` and OpenAPI doc-builder pattern); both the long form and the `-V` short alias work; argparse handles the print + exit before any other flag is processed (so `--version` short-circuits past mutual-exclusion checks); two new tests in `tests/test_cli.py` cover the long+short forms and the exit-0 contract; tiny conventional unix flag useful for bug reports / CI environment capture (closes feat-cli-version)
- feat(api): add `GET /api/shape-styles` endpoint — narrower JSON sibling of `/api/styles` returning only the three core shape enums (`hull_styles`, `engine_styles`, `wing_styles`) mirroring the CLI `--list-shape-styles` flag (`c9aefd8`); enum values emitted in enum-declaration order via the same `[s.value for s in HullStyle]` serialization used by `/api/styles` so the two endpoints stay byte-identical for the shared keys (asserted by `test_api_shape_styles_matches_styles_subset`); new `ShapeStyles` schema in `_OPENAPI_COMPONENTS` and `/api/shape-styles` path in `_OPENAPI_PATHS` so `/api/spec` enumerates the route and `tests/test_api_spec_validate.py` stays green; three new tests in `tests/test_api.py` cover 200 + content-type + non-empty arrays, drift-vs-`/api/styles`, and presence in `/api/spec` (closes feat-api-shape-styles)
- feat(scripts): add `scripts/bench_mem.py` — peak-memory micro-benchmark wrapping a full `generate()` call (shape + texture + weapons + `.litematic` write to a tmpdir) in `tracemalloc.get_traced_memory()` over N iterations; argparse `--iterations N` (default 5), `--seed S`, `--palette NAME` mirroring `bench_full_pipeline.py`/`bench_shape.py`; prints a fixed-width `pipeline / TOTAL` table with mean/p95/max MB; `tracemalloc.reset_peak()` is called between iterations so each sample reflects only that iteration's peak Python heap (not cumulative high-water); stdlib only (no `psutil`, no `pympler`) so it runs identically across the OS/Python matrix; `tests/test_bench_smoke.py::test_bench_mem_runs_with_two_iterations` runs the script with `--iterations 2 --seed 0` via `subprocess.run` and asserts exit 0 + presence of `pipeline`/`TOTAL`/`mean_mb`/`p95_mb`/`max_mb` in stdout; complements `bench_shape.py` (per-stage time) and `bench_full_pipeline.py` (end-to-end time) by adding the memory dimension as a foundation for `shapes-A`..`shapes-D` mem-budget work (closes feat-bench-mem)
- test(api): harden `/api/palettes/<name>` test coverage (closes feat-api-palette-detail) — `tests/test_api.py::test_api_palette_detail_ok` now asserts `Content-Type` starts with `application/json` (matches the `test_api_health_ok` / `test_api_spec_status_and_content_type` style); `test_api_palette_detail_not_found` asserts the same on the 404 path so the error body's media type is locked in too; new `test_api_palette_detail_listed_in_openapi_spec` pins `/api/palettes/{name}` (with `200` + `404` responses) directly in `/api/spec`'s `paths` dict so a refactor that drops the entry fails by name rather than via the generic `test_api_spec_lists_every_route` diff; the route + OpenAPI `PaletteDetail` schema themselves shipped earlier in `ea80439`, so this entry closes the todo unit by aligning the test surface with the acceptance criteria
- docs(cli): add `docs/cli.md` — full CLI flag-reference catalog grouping every `add_argument` declaration in `src/spaceship_generator/cli.py::build_parser` (in declaration order) into thematic sections (Identity, Seed, Palette, Style discovery, Presets, Shape params, Texture, Weapons, Repeat & fleet, Dry-run, Output, Preview, Verbosity, Diagnostics) using the same Markdown-table style as `docs/palettes.md` (Flag | Type/Value | Default | Description); one-line link added to the existing `### Key flags` subsection of `README.md` (no restructure); complements `--help` with a stable, link-friendly reference page (closes feat-docs-cli-reference)
- feat(scripts): add `scripts/bench_full_pipeline.py` — end-to-end micro-benchmark wrapping a full `generate()` call (shape + texture + weapons + `.litematic` write to a tmpdir) in `time.perf_counter()` over N iterations; argparse `--iterations N` (default 50), `--seed S`, `--palette NAME` mirroring `bench_shape.py`; prints a fixed-width `pipeline / TOTAL` table with mean/p95/total ms; numpy + stdlib only (no matplotlib, no pandas); `tests/test_bench_smoke.py::test_bench_full_pipeline_runs_with_two_iterations` runs the script with `--iterations 2 --seed 0` via `subprocess.run` and asserts exit 0 + presence of `pipeline`/`TOTAL`/`mean_ms`/`p95_ms`/`total_ms` in stdout; complements `bench_shape.py` (shape-only stages) by giving the end-to-end ship-build budget for `shapes-A`..`shapes-E` perf work
- feat(api): add `GET /api/health` endpoint — extends the existing health view with `uptime_s` (integer seconds since blueprint import via a module-level `time.monotonic()` baseline) alongside the legacy `status`/`version`/`palette_count`/`preset_count` keys; sets `Cache-Control: no-store` so liveness probes always see fresh state; `version` sourced from `spaceship_generator.__version__` (matches the existing `api_meta` and OpenAPI doc-builder pattern); OpenAPI `Health` schema in `_OPENAPI_COMPONENTS` updated to declare `uptime_s` (`type: integer, minimum: 0`) so `/api/spec` enumerates it and `tests/test_api_spec_validate.py` stays green; new `tests/test_api.py::test_api_health_ok` and `test_api_health_no_store_cache_control` cover the contract; standard k8s liveness/readiness pattern
- feat(cli): add `--quiet`/`-q` flag — suppresses all stdout on the success path (including `--list-palettes`/`--list-presets`/`--list-styles`/`--list-shape-styles`/`--list-weapon-types`, `--dry-run` JSON, `--stats`, `--block-summary`, and `--palette-info`) by funneling every emitter through a new `_emit(args, msg)` helper that no-ops under `--quiet`; stderr (errors, warnings, the `--seed-phrase` breadcrumb, `presets/weapon_styles unavailable` fallbacks) is never silenced; `--output-json` (machine-readable NDJSON side-channel) deliberately keeps emitting under `--quiet` because four pre-existing tests already pair `--quiet --output-json` and rely on it as a documented contract — this exception is documented in the `--quiet` help text; three new tests in `tests/test_cli.py` cover `--quiet --dry-run` empty-stdout, the regression guard for plain `--dry-run` still emitting, and the `-q` short alias + argparse-error stderr/empty-stdout path
- docs(palettes): add `docs/palettes.md` — catalog listing all 49 shipped palettes (the count is 49, not the stale 46 in older docs) in a 2-column Markdown table sourced directly from each yaml's `description:` field, alphabetical by palette name; matches the existing 3-column palette grid in `README.md` so the docs read consistently; one-line link added to the `Palettes` section of `README.md` directly above the existing grid; references `docs/palette_authoring.md` for contributors (no `CONTRIBUTING.md` exists)
- feat(palettes): add `cherry_grove`, `sparse_jungle` palettes — biome pack for Minecraft 1.20 cherry grove (cherry-planks hull, cherry-log dark, pink-stained-glass windows, stripped-cherry-log engines, shroomlight engine glow, white-stained-glass cockpit, pink-petals wings, stripped-cherry-wood greebles, lantern lights, pink-wool interior) and sparse jungle (jungle-log hull, stripped-jungle-log dark, lime-stained-glass windows, jungle-planks engines, ochre-froglight engine glow, green-stained-glass cockpit, jungle-leaves wings, stripped-jungle-wood greebles, lantern lights, moss-block interior); both pass strict palette lint (WINDOW luminance, HULL/HULL_DARK contrast, ENGINE_GLOW emissive)
- fix(weapons): make weapon writer truly additive — new `_nose_tip_anchor_cells()` helper in `src/spaceship_generator/generator.py` mirrors the centerline nose-tip geometry used by `texture._paint_nose_tip_light`, and the weapon write loop now refuses to stamp HULL/ENGINE_GLOW directly above any nose-tip slot; root cause was that weapons stamped legitimately into `Role.EMPTY` cells above the nose tip, which then caused `_paint_nose_tip_light` to bail (top cell was a `_PROTECTED_ROLES` member) and silently drop the nose-tip LIGHT, breaking the `var_weapon_cells >= base_weapon_cells` invariant for seed=93 wc=4; new `tests/test_generator.py::test_generate_weapon_writer_does_not_shadow_nose_tip_light` regression test pinned to that falsifying example so a future regression fails fast without needing Hypothesis to re-discover (closes bug-weapon-count-decreases-cells-2026-04-27)
- test(api): validate `/api/spec` response against the OpenAPI 3.0 meta-schema — new `tests/test_api_spec_validate.py` fetches the live document via the Flask test client and runs `jsonschema.Draft4Validator` against the official OAS 3.0 schema (2021-09-28 release) vendored at `tests/fixtures/openapi-3.0-schema.json` so the test runs offline; catches drift if a future endpoint omits `responses`/`summary` or breaks the `info`/`paths` shape; `pytest.importorskip("jsonschema")` keeps the suite green when the dep is missing, and `requirements-dev.txt` now declares `jsonschema>=4.0` for CI environments that consume that file
- feat(scripts): add `scripts/bench_shape.py` — per-stage micro-benchmark for the shape pipeline; `--iterations N` (default 50), `--seed S`, `--palette NAME` flags; wraps each public stage helper (`_place_hull`, `_place_cockpit`, `_place_engines`, `_place_wings`, `_place_greebles`, then assembly = `_enforce_x_symmetry` -> `_connect_floaters` -> `_enforce_x_symmetry`) in `time.perf_counter()` and prints a one-table mean/p95/total summary in milliseconds; numpy + stdlib only (no matplotlib, no pandas); `tests/test_bench_smoke.py` runs the script with `--iterations 2` via `subprocess` to guard against import/argparse regressions; foundation for `shapes-A`..`shapes-E` perf work
- feat(cli): add `--list-shape-styles` flag — prints `HullStyle`, `EngineStyle`, `WingStyle` members grouped (one section per enum, one member per indented line) in enum-declaration order, then exits 0; narrower sibling of `--list-styles` (which also includes cockpit + weapon types) so users can grep/diff just the three core shape enums; existing `--list-styles` output unchanged
- feat(shape): add `--hull-noise AMPLITUDE` flag — procedural-noise hull distortion (asteroid-pitted / battle-damaged look) via a deterministic 3D hash-noise post-pass on the hull membrane; amplitude in `[0.0, 1.0]` (default `0.0` is a no-op and byte-identical to the legacy pipeline), clamped to ±2 cells of silhouette displacement so the ship stays legible; deterministic per seed (the noise sub-seed is drawn from the existing `ShapeParams` rng); new `ShapeParams.hull_noise` field with validation; numpy-vectorized, no extra deps (shapes-E)
- feat(palettes): add `soul_sand_valley`, `savanna_acacia` palettes — biome pack for the nether soul-sand valley (soul-soil hull, blackstone underframe, bone-block wings, soul-lantern engine glow, soul-torch running lights) and savanna acacia (smooth-sandstone hull, acacia-log accent, acacia-plank wings, ochre-froglight engine glow, lantern running lights)
- feat(shape): add `--hull-style-front`/`--hull-style-rear` flags — blend two `HullStyle` profiles along Z with a cosine-weighted crossover centred at z = L/2 over a 25% midband; both flags must be set to engage the blend (a partial pair silently falls back to the legacy single-style hull); when both are set the blend overrides `--hull-style`; new `apply_hull_blend()` library helper and `generate_shape(..., hull_style_front=, hull_style_rear=, hull_blend_midband=)` kwargs are deterministic per-seed (TODO: gallery sample render pending manual rendering tooling) (shapes-B)
- feat(palettes): add `lush_caves`, `mangrove_swamp`, `pale_garden` palettes — biome pack for Minecraft 1.18 lush caves (moss-block hull, azalea-leaves accent, verdant-froglight glow, glow-lichen lights), 1.19 mangrove swamp (mangrove-log hull, muddy-mangrove-roots accent, ochre-froglight glow), and 1.21 pale garden (pale-oak-log hull, pale-moss-block accent, creaking-heart engines, lantern glow)
- feat(api): add `GET /api/spec` endpoint — returns a hand-written OpenAPI 3.0.3 JSON document enumerating every `/api/*` route (paths, methods, summaries, query/path params, 200 response schemas) for client codegen and Swagger/Redoc browsers; version sourced from package metadata
- docs(architecture): document shape pipeline — new `## Shape pipeline` section in `docs/architecture.md` with a Mermaid build-order diagram (hull -> cockpit -> engines -> wings -> greebles -> mirror -> connect-floaters -> mirror) and one subsection per module in `src/spaceship_generator/shape/` (`core.py`, `hull.py`, `assembly.py`, `cockpit.py`, `wings.py`, `engines.py`, `greebles.py`); foundation for the `shapes-A`..`shapes-E` epic
- feat(cli): add `--from-manifest FILE` flag — reproduce a ship from a prior `--export-manifest` JSON sidecar; validates required keys (seed/palette/shape) and rejects conflicts with `--seed`/`--seeds`/`--seed-phrase`/`--repeat`/`--fleet-count`
- feat(api): add `GET /api/random` endpoint — returns a random `{seed, palette, preset}` JSON triple for client-side spin-the-wheel; optional `?seed=<int>` query param makes palette/preset selection reproducible; `Cache-Control: no-store`
- feat(cli): add `--no-greebles` shortcut — equivalent to `--greeble-density 0`; mutually exclusive with `--greeble-density` (errors with non-zero exit if both passed)
- feat(palettes): add `ancient_city` palette — deepslate-tile hull, cracked-deepslate accent, sculk-catalyst engine glow, soul-lantern running lights (deep dark biome / ancient city theme)
- feat(palettes): add `dripstone_cave` palette — dripstone-block hull, pointed-dripstone accent, copper-bulb warm engine glow, cut-copper engines, weathered-copper wings (Minecraft dripstone cave biome)
- test(cli): pin `--repeat N` produces exactly N distinct `.litematic` files with byte-distinct contents (4-ship distinctness property test, runs ~0.07s)
- fix(ci): commit 105 block-texture PNG cache files and add `.gitignore` exception — resolves `test_block_texture_png_returns_cached_bytes` and two related CI failures on all Python/OS matrix combinations
- feat(api): add `GET /api/styles` endpoint — returns `hull_styles`, `engine_styles`, `wing_styles`, `greeble_types`, `weapon_types` arrays for client-side discovery (parity with `/api/palettes` and `/api/presets`)
- feat(cli): add `--dry-run` flag — resolves and prints generation params (seed, palette, dims, preset) as JSON without writing files; exits 0
- feat(cli): add `--greeble-style TYPE` flag — restricts greeble scatter to one named `GreebleType` (turret/dish/vent/antenna/panel_line/sensor_pod); threads through `generate()` via new `greeble_types` param
- feat(palettes): add `badlands_mesa` palette — red terracotta hull, orange terracotta wings, copper block engine, ochre froglight glow (Minecraft badlands/mesa biome theme)
- feat(palettes): add `end_city` palette — purpur block hull, end stone brick accent, end rod glow (The End dimension / End City theme)
- docs(readme): update palette count (33→40), hull silhouette count (5→10), engine style count (5→9); add missing `--ship-size`, `--seed-phrase`, `--export-manifest`, `--palette-info` to key-flags table; expand Styles section with all current hull/engine variants

- fix(cli): replace `utcnow()` with timezone-aware `datetime.now(UTC)` in `--export-manifest` timestamp — eliminates DeprecationWarning on Python 3.12+
- fix(tests): add pytest `filterwarnings` entry to suppress known-harmless runpy `RuntimeWarning` in `test_cli_module_runpy_smoke`
- feat(palettes): add `mushroom_islands` palette — mycelium hull, red mushroom cap wings, shroomlight engine glow (mushroom island biome theme)
- feat(palettes): add `prismarine_sea` palette — prismarine-brick hull, dark prismarine underframe, sea-lantern engine glow (ocean monument theme)
- feat(cli): add `--ship-size WxHxL` flag — override ship dimensions at generation time; validates W>=4, H>=4, L>=8; overrides preset size
- feat(api): add `GET /api/compare` endpoint — accepts `seed_a`/`seed_b` plus optional `palette`/`preset`; returns both ships' dims, voxel count, and role counts side-by-side without generating files

- fix(cli): replace `→` with `->` in `--stats` help and `--seed-phrase` stderr output — fixes `UnicodeEncodeError` on Windows cp1252 terminals
- feat(cli): add `--palette-info <name>` flag — prints each role with its block ID and hex preview color for the named palette; exits 0 on success, 1 on unknown name
- feat(cli): add `--export-manifest` flag — writes a `<name>.json` sidecar alongside each generated `.litematic` containing seed, palette, shape, block count, and UTC timestamp
- feat(api): add `GET /api/fleet/plan` endpoint — returns JSON fleet metadata (count, size tier, coherence, per-ship seed/dims/styles) without generating any files; validates all five query params with 400 on bad input
- feat(palettes): add `jungle_canopy` palette — jungle log hull, mossy cobblestone underframe, ochre froglight engine glow, lime glass windows (tropical jungle theme)
- feat(palettes): add `swamp_bog` palette — mangrove log hull, mud underframe, verdant froglight engine glow, soul lantern running lights (dark swamp theme)

- feat(web): add seed-phrase text input to web form — type a phrase to get a deterministic seed (SHA-256, same algorithm as `--seed-phrase`); overrides the numeric seed field
- feat(api): add `GET /api/presets/<name>` endpoint — returns full single-preset detail (symmetry with `/api/palettes/<name>`); 404 on unknown name
- feat(cli): add `--list-weapon-types` flag — prints all 5 weapon type names with 2-space indent; completes the `--list-*` discovery family alongside `--list-palettes`, `--list-styles`, `--list-presets`

- feat(presets): add `description` field to all 9 ship archetypes — shown in `--list-presets` output and `/api/presets` JSON response
- feat(api): add `GET /api/palettes/<name>` endpoint — returns roles, block IDs, and hex preview colors for a single named palette; 404 on unknown name
- feat(cli): add `--seed-phrase <text>` flag — hashes text to deterministic seed (SHA-256 mod 2^31-1); prints resolved integer seed so ships can be reproduced
- feat(palettes): add `cherry_blossom` palette — cherry planks hull, pink concrete accent, shroomlight glow (Minecraft 1.20 cherry blossom theme)
- feat(api): add `GET /api/presets` endpoint — returns full preset metadata (hull/engine/wing/cockpit styles, size, weapon config) as JSON for all 9 archetypes
- feat(cli): add `--repeat N` flag — generates N ships with consecutive seeds from a base seed; mutually exclusive with `--seeds`
- feat(palettes): add `frozen_tundra` palette — calcite hull, packed-ice engine mass, soul-lantern cold-blue glow (arctic tundra theme)
- feat(api): add `GET /api/health` health-probe endpoint — returns `{"status":"ok","version":"...","palette_count":N,"preset_count":M}` for Docker/orchestration
- feat(palettes): add `volcanic_ash` palette — tuff hull, basalt engine casing, glowstone ember glow (volcanic ash theme)

- feat(presets): add `scout`, `battlecruiser`, `capital_carrier` ship archetypes — small fast recon, heavy combat cruiser, and massive fleet carrier presets
- feat(palettes): add `nebula_drift` palette — deep purple/purpur hull, magenta glass windows, amethyst engine block with shroomlight glow
- feat(palettes): add `solar_flare` palette — yellow/orange terracotta hull, gold-block wings, glowstone engine wash and lantern running lights
- feat(palettes): add `abyss_deep` palette — deepslate-brick hull, dark prismarine engines, soul-lantern running lights in cold deep-ocean blue
- feat(web): add DIMS (W×H×L) ship dimensions readout to viewport status bar alongside VOX and FPS
- feat(cli): add `--block-summary` flag — prints `block_id,count` CSV sorted by count descending for survival mode resource planning

- feat(cli): add `--preview-azimuth` and `--preview-elevation` flags to control isometric camera angle when saving previews
- feat(palettes): add `autumn_harvest` palette — orange terracotta hull, brown terracotta dark, shroomlight engine glow, jack o'lantern lights (seasonal theme)
- feat(web): add keyboard shortcut help overlay — press `?` (or click the `?` button) to toggle a modal listing all shortcuts; styled with neon accent border
- feat(cli): add `--palette random` alias — resolves to a randomly chosen palette at generation time
- feat(web): add ⟳ Random Palette button inline with the palette selector; updates swatch strip on click

- feat(web): palette color swatches in UI — `/api/palettes` now returns hex preview colors; swatch strip renders below palette selector
- feat(api): `GET /api/result/<gen_id>` endpoint returns full result metadata (seed, palette, shape, blocks, filename, download/preview URLs)
- feat(cli): `--output-json` flag emits NDJSON summary (seed, palette, shape, blocks, path) to stdout after each generation
- feat(api): `POST /api/batch` endpoint generates 1–10 ships per request; `api_meta` exposes `batch_max: 10`
- fix(test): replaced private `app.config["_RESULTS"]` access in eviction test with new `/api/result/<id>` endpoint
- refactor(web): split monolithic app.js/preview.js/style.css into focused modules (app_core, app_ui, preview_math, preview_renderer, preview_bootstrap, style_base, style_controls, style_preview)
- fix(block_colors): replace deprecated `Image.getdata()` with `get_flattened_data()` (Pillow 12+)
- fix(web): form controls (styles/preset/density) now wire into the generator pipeline
- feat(palettes): add circus_bigtop (red/white main, gray support, blue accent)
- chore(lint): remove unused `json` import in `tests/test_web_ui.py` (ruff autofix)

### Fixed
- **Palettes**: tightened `preview_colors` to reduce `palette_lint.py` warnings from 36 to 23. Brightened dark WINDOW previews (`coral_reef`, `crimson_nether`, `rustic_salvage`, `stealth_black`, `steampunk_brass`) above the Y≥0.35 threshold and darkened HULL_DARK previews (`biopunk_fungal`, `candy_pop`, `crimson_nether`, `deepslate_drone`, `desert_sandstone`, `ice_crystal`, `nordic_scout`, `steampunk_brass`) to raise HULL contrast above 1.5. In-game block IDs are unchanged — only the 2D preview swatches were adjusted.

### Notes
- *Intentional "non-emissive ENGINE_GLOW" lint warnings are kept as design choices: `amethyst_crystal` (amethyst_block — crystal aesthetic), `candy_pop` (pink_glazed_terracotta — candy gloss), `end_void` (end_rod — end-dimension voidrunner motif), `rustic_salvage` (fire_coral_block — scrap-metal coral glow), `sleek_modern` (end_rod — minimalist accent).*
- *Intentional duplicate-role warnings (same block assigned to two roles) are preserved when they reflect palette identity — e.g. `amethyst_crystal` HULL/WING both calcite, `stealth_black` HULL/INTERIOR both black_concrete, `wooden_frigate` WINDOW/COCKPIT_GLASS both glass.*

## [0.2.0] - 2026-04-21

Wave-1 and wave-2 content expansion, performance, and infrastructure landings.

### Added
- **Hull styles**: ARROW, SAUCER, WHALE, DAGGER, BLOCKY_FREIGHTER silhouettes (`e70d2e0`).
- **Engine styles**: `EngineStyle` enum with 5 variants (`08dfe15`).
- **Wing styles**: `WingStyle` enum with 5 silhouettes — swept, delta, tapered, gull, split (`5a7ef95`).
- **Cockpit styles**: CANOPY_DOME, WRAP_BRIDGE, OFFSET_TURRET variants (`e93bbb0`).
- **Greeble styles**: 6 greeble types plus `scatter_greebles` helper (`efe67cc`).
- **Weapon styles**: `weapon_styles` library with 5 types plus scatter helper (`6e98599`).
- **Fleet generator**: coherent multi-ship planning (`0d0d69f`).
- **Palettes**: `steampunk_brass`, `biopunk_fungal`, `cyberpunk_neon` built-ins plus palette author guide (`8acbe19`).
- **Palette linter**: validator script plus tests (`caa451c`).
- **Generator integration**: HullStyle + EngineStyle + `scatter_greebles` wired into the pipeline (`daa0313`).
- **CLI flags**: `--hull-style`, `--engine-style`, `--wing-style`, `--greeble-density`, `--list-styles` (`34f7134`); weapon-count and fleet-generation flags (`0f4af3c`).
- **Web UI polish**: palette swatches, seed copy button, shortcuts help overlay, responsive layout, theme toggle (`81ee2b1`).
- **Web style pickers**: hull, engine, wing pickers plus greeble-density slider (`65c17ed`).
- **Web live preview**: `/preview-lite` endpoint with debounced live preview toggle (`d69af03`).
- **Preview quality**: supersampling antialias, specular highlights, transparent background (`c348db7`).
- **Smoke tests**: end-to-end smoke script plus `ci-subset` pytest wrapper (`59e6aa4`).
- **Property tests**: hypothesis-based property tests; line coverage raised from 86% to 97% (`2d53980`).
- **Benchmarking**: `bench_generator` script with baseline performance report (`3c7b839`); perf-bench PR-comment workflow plus compare script (`ae5744d`).
- **Docker**: Dockerfile, `.dockerignore`, and `docs/docker.md` (`52cd49a`).
- **Docs**: project FAQ (`d2679cc`); gallery generator script plus rendered showcase (`d96b365`); README refresh with new palettes, styles, CI badge, dev section (`48120da`).
- **CI/CD**: GitHub Actions workflows for CI and release, plus release-process documentation (`726f79b`).

### Changed
- **Shape module** split into submodules, each under the 500-line ceiling (`75f9d27`).
- **Web app** split from monolithic `app.py` into Flask blueprints, each under 500 lines (`ce1e7fd`).
- **Texture**: robust palette fallbacks plus tests for all 18 palettes (`3c9ff7b`).

### Performance
- **Shape**: vectorized `_label_components` — ~91% faster (`dea1ffc`).
- **Export**: cached `BlockState` by role — ~52% faster (`aace72c`).

### Fixed
- _Nothing in 0.2.0 was a pure user-facing fix — see 0.1.0 history for earlier fixes._

### Notes
- *Commit `efe67cc` uses a palette-style message but actually contains the `greeble_styles` module — content-only, not re-pushed.*

---

## [0.1.0] - Pre-release history

Seeded from the commit log prior to formal release tagging. Grouped by
conventional-commit type.

### Added (feat)
- WingStyle enum: 5 new wing silhouettes (swept / delta / tapered / gull / split).
- Web: sci-fi console UI revamp; restored pan/orbit signs.
- Structure styles, translucent blocks, slider controls.
- Web: WebGL preview, progress bar, random-ship button.
- Block-texture preview, async web UI, palette library.
- Cockpit styles, advanced texturing, CLI batch/preview, web API.
- Web: Flask UI for interactive generation.
- Preview: headless matplotlib isometric PNG renderer.
- Generator + CLI: end-to-end pipeline orchestrator and CLI.
- Texture: refine coarse shape grid into fine role assignments.
- Shape: parts-based procedural ship with mirror symmetry.
- Export: role grid to `.litematic` via `litemapy`.
- Palette: role enum, palette YAML loader, three built-in palettes.

### Fixed (fix)
- Web: one POST per Generate/Random click.
- Web: exempt loopback from rate limiting; bump default to 30/min.
- Web: unbroken sci-fi console UI; add rate limiting.
- 20-agent swarm audit (10 hunters + 10 fixers).
- Slider controls and panning behavior.
- Floater artifacts in WebGL preview.

### Chore
- Initial project scaffold.

[Unreleased]: https://github.com/KazooKat/Spaceship-Generator/compare/v0.2.0...HEAD
[0.2.0]: https://github.com/KazooKat/Spaceship-Generator/compare/v0.1.0...v0.2.0
[0.1.0]: https://github.com/KazooKat/Spaceship-Generator/releases/tag/v0.1.0
